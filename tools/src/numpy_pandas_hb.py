# NumPy & Pandas Handbook spec
SPEC = {
  "id": "numpy-pandas", "name": "NumPy & Pandas Handbook", "icon": "🐼",
  "tagline": "Master NumPy & Pandas for data/ML interviews: arrays, vectorization, broadcasting, DataFrames, groupby, joins, and performance.",
  "itemNoun": "Topic",
  "levels": ["Beginner", "Intermediate", "Advanced"],
  "categories": [
    {"name": "NumPy Core", "level": "Beginner", "icon": "🔢"},
    {"name": "NumPy Advanced", "level": "Intermediate", "icon": "📐"},
    {"name": "Pandas Core", "level": "Beginner", "icon": "🐼"},
    {"name": "Pandas Analytics", "level": "Intermediate", "icon": "📊"},
    {"name": "Performance", "level": "Advanced", "icon": "⚡"},
  ],
  "items": [
    {"id":1,"slug":"01-ndarray","name":"The ndarray","category":"NumPy Core","level":"Beginner",
     "summary":"A fast, fixed-type, N-dimensional array — the basis of the PyData stack.",
     "keywords":["ndarray","dtype","shape","contiguous","numpy"],"refs":["NumPy basics"],
     "overview":"NumPy's **ndarray** is a contiguous, fixed-**dtype**, N-dimensional array. Because elements are the same type and laid out contiguously in memory, operations run in optimized C — orders of magnitude faster than Python lists.",
     "concepts":["Homogeneous dtype (int64, float64, bool) — not mixed like lists.","shape describes dimensions; ndim the count.","Contiguous memory → cache-friendly, vectorizable.","dtype controls precision and memory footprint.","Created via array, zeros, ones, arange, linspace."],
     "code":[("python","import numpy as np\na = np.array([[1, 2, 3], [4, 5, 6]], dtype=np.int64)\nprint(a.shape, a.ndim, a.dtype)  # (2, 3) 2 int64\nprint(np.arange(0, 1, 0.25))     # [0.   0.25 0.5  0.75]")],
     "example":{"title":"Lists vs arrays speed","text":"Vectorized math beats Python loops:","code":[("python","x = np.arange(1_000_000)\ny = x * 2 + 1          # vectorized, runs in C\n# vs [i*2+1 for i in range(1_000_000)]  # much slower")]},
     "best":["Pick the smallest dtype that fits (memory + speed).","Prefer vectorized ops over Python loops.","Know shape/ndim before operating.","Use np.zeros/empty to preallocate.","Keep arrays contiguous for performance."],
     "pitfalls":["Mixing dtypes forcing an object array (slow).","Integer overflow with small int dtypes.","Assuming arrays are dynamically resizable (they're fixed-size).","float precision surprises (use np.isclose).","Confusing ndim with len.","Accidentally creating object dtype from ragged lists."],
     "interview":[("Why is ndarray faster than a list?","Homogeneous dtype + contiguous memory enable vectorized C loops and cache efficiency."),("What is a dtype?","The fixed element type (e.g., int64, float32) determining size and precision."),("shape vs ndim?","shape is the tuple of dimension sizes; ndim is the number of dimensions."),("What happens with mixed types?","NumPy falls back to object dtype, losing the speed advantage."),("Are arrays resizable?","No — they're fixed-size; 'append' creates a new array (costly)."),("How to preallocate?","np.zeros/np.empty with the target shape and dtype."),("Why choose float32 over float64?","Half the memory and often faster, at reduced precision."),("How to compare floats?","np.isclose / np.allclose, not ==.")],
     "practice":["Create a 2D int64 array and inspect shape/dtype.","Benchmark a vectorized op vs a Python loop.","Trigger and explain an object-dtype array."],
     "revision":"ndarray = contiguous, fixed-dtype, N-D array → fast vectorized C ops. Pick small dtypes, vectorize, preallocate, keep contiguous; avoid mixed types (object dtype) and float ==."},

    {"id":2,"slug":"02-indexing-slicing","name":"Indexing, Slicing & Views","category":"NumPy Core","level":"Beginner",
     "summary":"Slices are views (no copy); fancy/boolean indexing copies.",
     "keywords":["indexing","slicing","view","copy","boolean mask","fancy"],"refs":["Indexing"],
     "overview":"Basic **slices return views** that share memory with the original (mutations propagate). **Fancy indexing** (integer/boolean arrays) returns **copies**. Boolean masks are the idiomatic way to filter data.",
     "concepts":["a[1:3, :] is a view — editing it edits a.","Boolean mask a[a > 0] selects matching elements (copy).","Fancy indexing a[[0,2]] copies selected rows.","Negative indices count from the end.","Use .copy() to detach a view."],
     "code":[("python","a = np.arange(10)\nview = a[2:5]\nview[0] = 99       # also changes a[2]!\nmask = a > 5\nprint(a[mask])     # boolean filtering -> copy")],
     "example":{"title":"Conditional replace","text":"Use a mask to assign in place:","code":[("python","a = np.array([-1, 2, -3, 4])\na[a < 0] = 0       # clamp negatives -> [0 2 0 4]")]},
     "best":["Use boolean masks for filtering — clear and fast.","Call .copy() when you need independence from a view.","Prefer slicing over loops for subsets.","Combine masks with & / | (parenthesized).","Use np.where for vectorized if/else."],
     "pitfalls":["Mutating a view and unintentionally changing the source.","Using Python and/or instead of & / | on arrays.","Forgetting parentheses around mask conditions.","Assuming fancy indexing returns a view (it copies).","Out-of-bounds fancy indices raising errors.","Chained indexing causing copy-vs-view ambiguity."],
     "interview":[("View vs copy in NumPy?","Basic slicing returns a view sharing memory; fancy/boolean indexing returns a copy."),("How to filter with a condition?","Boolean mask: a[a > k]."),("Why use & / | not and/or?","Python's and/or operate on truth values; element-wise needs & / | with parentheses."),("How to make a slice independent?","Call .copy() on it."),("What does np.where do?","Vectorized conditional selection/assignment (if/else over arrays)."),("Does a[[0,2]] return a view?","No — integer (fancy) indexing copies."),("How to assign to a masked subset?","a[mask] = value updates matching elements in place."),("Risk of chained indexing?","It can produce a temporary copy, so the assignment may not stick.")],
     "practice":["Show a view mutating its source, then fix with copy.","Clamp negatives to zero with a boolean mask.","Use np.where to build an if/else array."],
     "revision":"Slices = views (shared memory); fancy/boolean indexing = copies. Filter with masks (& / | + parens), detach with .copy(), branch with np.where; beware mutating views."},

    {"id":3,"slug":"03-vectorization","name":"Vectorization & ufuncs","category":"NumPy Core","level":"Beginner",
     "summary":"Replace Python loops with array-wide C operations.",
     "keywords":["vectorization","ufunc","elementwise","loop","speed"],"refs":["Universal functions"],
     "overview":"**Vectorization** expresses computations as operations over whole arrays, executed by C-level **ufuncs** (np.add, np.exp, etc.). It's the single biggest NumPy performance lever — often 10–100× faster than Python loops and far more concise.",
     "concepts":["ufuncs apply element-wise in compiled C.","Arithmetic/comparison operators are ufuncs.","Reductions (sum/mean/max) collapse axes.","Avoid Python-level loops over elements.","np.vectorize is convenience, not speed."],
     "code":[("python","x = np.linspace(0, np.pi, 5)\ny = np.sin(x) ** 2 + np.cos(x) ** 2   # vectorized, ~1.0 everywhere\ntotal = y.sum()")],
     "example":{"title":"Loop → vectorized","text":"Distance between point arrays without a loop:","code":[("python","p = np.random.rand(1000, 2)\nq = np.random.rand(1000, 2)\nd = np.sqrt(((p - q) ** 2).sum(axis=1))   # 1000 distances, no loop")]},
     "best":["Express math over whole arrays, not element loops.","Use built-in ufuncs/reductions.","Specify axis explicitly in reductions.","Combine masks + vectorized ops for conditional logic.","Profile before micro-optimizing."],
     "pitfalls":["Python loops over array elements (slow).","Thinking np.vectorize gives C speed (it doesn't).","Wrong axis in reductions.","Creating large temporaries in long expressions (memory).","Mixing Python floats and arrays unnecessarily.","Forgetting integer division vs float behavior."],
     "interview":[("What is vectorization?","Performing operations on entire arrays via compiled ufuncs instead of Python loops."),("Why is it faster?","It avoids the Python interpreter overhead per element and uses CPU/cache-friendly C loops."),("What is a ufunc?","A universal function applying an operation element-wise across arrays (with broadcasting)."),("Does np.vectorize speed things up?","No — it's a convenience wrapper; the loop is still Python-level."),("How do reductions work?","Functions like sum/mean collapse a chosen axis to aggregate."),("How to do conditional logic vectorized?","Boolean masks and np.where."),("Memory cost of big expressions?","Intermediate temporaries; chunk or use in-place ops if needed."),("axis=0 vs axis=1?","axis=0 reduces down columns; axis=1 across rows.")],
     "practice":["Rewrite a Python sum loop as a vectorized reduction.","Compute pairwise distances without loops.","Use a mask + np.where for conditional math."],
     "revision":"Vectorize: whole-array ufuncs in C beat Python loops 10–100×. Use built-in ufuncs/reductions (mind axis), masks/np.where for conditionals; np.vectorize is not fast."},

    {"id":4,"slug":"04-broadcasting","name":"Broadcasting","category":"NumPy Advanced","level":"Intermediate",
     "summary":"Operate on different-shaped arrays without copying.",
     "keywords":["broadcasting","shape","axis","dimensions","newaxis"],"refs":["Broadcasting"],
     "overview":"**Broadcasting** lets NumPy combine arrays of different shapes by virtually stretching dimensions of size 1 — no data is copied. It powers concise, memory-efficient operations like normalizing rows or computing outer products.",
     "concepts":["Align shapes from the right; dims must be equal or 1.","Size-1 dimensions stretch to match.","np.newaxis/reshape adds axes to enable broadcasting.","No actual data duplication — it's virtual.","Mismatched, non-1 dims raise a broadcast error."],
     "code":[("python","A = np.arange(6).reshape(2, 3)   # (2,3)\ncol = np.array([10, 20])         # (2,)\nresult = A + col[:, np.newaxis]  # (2,1) broadcasts to (2,3)")],
     "example":{"title":"Normalize each row","text":"Subtract per-row mean using broadcasting:","code":[("python","X = np.random.rand(100, 5)\nXn = X - X.mean(axis=1, keepdims=True)   # (100,1) broadcasts over (100,5)")]},
     "best":["Use keepdims=True to keep broadcastable shapes.","Add axes with np.newaxis/reshape deliberately.","Prefer broadcasting over tiling (saves memory).","Verify shapes when results look wrong.","Use outer broadcasting for pairwise computations."],
     "pitfalls":["Shape mismatch errors from misaligned dims.","Forgetting keepdims, breaking the broadcast.","Unintended broadcasting producing huge arrays (memory blowup).","Assuming row vs column vector orientation.","Silent wrong results when shapes broadcast unexpectedly.","Confusing (n,) with (n,1)."],
     "interview":[("What is broadcasting?","Implicitly stretching size-1 dimensions so arrays of different shapes can be combined without copying."),("Broadcasting rules?","Align shapes from the right; each dim must be equal or one of them is 1."),("Why keepdims=True?","Retains reduced axes as size-1 so the result broadcasts back over the original."),("How to add an axis?","np.newaxis or reshape, e.g., v[:, np.newaxis]."),("Does broadcasting copy data?","No — it's virtual; memory isn't duplicated."),("(n,) vs (n,1)?","1D vs a 2D column vector — orientation changes how broadcasting aligns."),("Risk of accidental broadcasting?","Creating a huge result array (e.g., (n,1) op (1,m) → (n,m))."),("Outer product via broadcasting?","a[:, None] * b[None, :].")],
     "practice":["Add a per-column offset via broadcasting.","Row-normalize a matrix with keepdims.","Build a pairwise difference matrix with newaxis."],
     "revision":"Broadcasting stretches size-1 dims (align from right; equal or 1) without copying. Use keepdims + newaxis to shape things; watch for accidental huge results and (n,) vs (n,1)."},

    {"id":5,"slug":"05-aggregations-axes","name":"Aggregations & Axes","category":"NumPy Advanced","level":"Intermediate",
     "summary":"Reduce arrays along axes; handle NaNs correctly.",
     "keywords":["sum","mean","axis","argmax","nan"],"refs":["Statistics functions"],
     "overview":"Reductions (sum, mean, std, min/max, argmax) collapse an array along an **axis**. Choosing the right axis is the key skill; `nan*` variants ignore missing values, and `arg*` returns indices of extrema.",
     "concepts":["axis=0 reduces across rows (per column); axis=1 per row.","None (default) reduces the whole array to a scalar.","argmax/argmin give positions, not values.","nanmean/nansum skip NaNs.","cumsum/cumprod give running aggregates."],
     "code":[("python","A = np.array([[1, 2, 3], [4, 5, 6]])\nprint(A.sum(axis=0))   # [5 7 9]  per column\nprint(A.sum(axis=1))   # [6 15]   per row\nprint(A.argmax())      # 5 (flattened index of max)")],
     "example":{"title":"NaN-aware stats","text":"Average ignoring missing data:","code":[("python","x = np.array([1.0, np.nan, 3.0])\nprint(np.nanmean(x))   # 2.0  (ignores NaN)\nprint(x.mean())        # nan  (propagates)")]},
     "best":["Always be explicit about axis.","Use nan-aware functions when data has gaps.","Use keepdims to retain shape for broadcasting.","Prefer arg* for locating extrema.","Validate axis with small examples."],
     "pitfalls":["Wrong axis flipping rows/columns silently.","Plain mean/sum propagating NaN to the whole result.","Confusing argmax (index) with max (value).","Flattened argmax index vs per-axis index.","Forgetting keepdims when chaining broadcasts.","Integer overflow in large sums (use a bigger dtype)."],
     "interview":[("axis=0 vs axis=1?","axis=0 aggregates down columns; axis=1 across each row."),("Default axis?","None — reduces the entire array to a scalar."),("argmax vs max?","argmax returns the index of the maximum; max returns the value."),("How to ignore NaNs?","Use nansum/nanmean/etc., which skip NaN entries."),("Why does mean return NaN?","Any NaN propagates through standard reductions."),("How to get per-axis argmax?","Pass axis to argmax; note it indexes within that axis."),("Running totals?","cumsum/cumprod."),("keepdims purpose in aggregation?","Preserve reduced dims as size-1 for later broadcasting.")],
     "practice":["Compute per-column and per-row sums.","Use nanmean on data with missing values.","Find the index of the max with argmax."],
     "revision":"Reductions collapse along axis (0=cols, 1=rows, None=all). arg* gives indices; nan* ignores NaN (plain ops propagate it); keepdims aids broadcasting. Always state the axis."},

    {"id":6,"slug":"06-dataframe-series","name":"DataFrame & Series","category":"Pandas Core","level":"Beginner",
     "summary":"Labeled 2D tables (DataFrame) of labeled 1D columns (Series).",
     "keywords":["dataframe","series","index","columns","dtypes"],"refs":["Pandas intro"],
     "overview":"Pandas adds **labels** to NumPy: a **Series** is a labeled 1D array; a **DataFrame** is a labeled 2D table of Series sharing an **index**. Labels (index + columns) enable intuitive selection, alignment, and joins.",
     "concepts":["DataFrame = dict-like collection of columns (Series).","The index labels rows; columns label fields.","Each column has its own dtype.","Operations auto-align on the index.","Construct from dicts, lists, CSV, SQL, etc."],
     "code":[("python","import pandas as pd\ndf = pd.DataFrame({\n    'name': ['Ann', 'Bob', 'Cy'],\n    'age': [30, 25, 35],\n    'city': ['NYC', 'LA', 'NYC'],\n})\nprint(df.dtypes)\nprint(df['age'].mean())   # 30.0")],
     "example":{"title":"Index alignment","text":"Operations align on labels, not position:","code":[("python","s1 = pd.Series([1, 2], index=['a', 'b'])\ns2 = pd.Series([10, 20], index=['b', 'c'])\nprint(s1 + s2)   # a:NaN, b:12, c:NaN  (aligned by label)")]},
     "best":["Set a meaningful index for alignment/lookups.","Check dtypes early (object columns are slow).","Use vectorized column ops, not row loops.","Load with proper parse options (dates, dtypes).","Inspect with head/info/describe."],
     "pitfalls":["Iterating rows with iterrows (slow) instead of vectorizing.","Object dtype columns from mixed/strings hurting speed.","Forgetting label-based alignment introduces NaNs.","Mutating a slice (SettingWithCopyWarning).","Default RangeIndex when a key index is better.","Ignoring memory of large object columns."],
     "interview":[("Series vs DataFrame?","Series is a labeled 1D array; DataFrame is a 2D table of Series sharing an index."),("What is the index?","Row labels enabling alignment, fast lookup, and joins."),("How do operations align?","By matching index/column labels, inserting NaN where labels don't overlap."),("Why are object dtypes slow?","They hold Python objects (e.g., strings), losing vectorization benefits."),("How to avoid row loops?","Use vectorized column operations / apply sparingly / groupby."),("What is SettingWithCopyWarning?","A warning that you may be assigning to a copy (a view/copy ambiguity)."),("How to inspect a DataFrame?","head(), info(), describe(), dtypes."),("Can columns have different dtypes?","Yes — each column is a typed Series.")],
     "practice":["Build a DataFrame from a dict and compute a column mean.","Demonstrate label alignment producing NaNs.","Set and use a custom index for lookups."],
     "revision":"Series=labeled 1D, DataFrame=labeled 2D table sharing an index. Ops align by label (NaN on mismatch). Set good indexes, mind object dtypes, vectorize over row loops."},

    {"id":7,"slug":"07-selection-filtering","name":"Selection: loc, iloc & Filtering","category":"Pandas Core","level":"Beginner",
     "summary":"Select by label (loc), position (iloc), and boolean masks.",
     "keywords":["loc","iloc","boolean","filter","query"],"refs":["Indexing and selecting"],
     "overview":"Pandas offers explicit selection: **.loc** (label-based), **.iloc** (integer position), and **boolean masks** for filtering rows by condition. Using these correctly avoids the chained-indexing pitfalls that cause subtle bugs.",
     "concepts":[".loc[rows, cols] selects by labels (inclusive ranges).",".iloc[rows, cols] selects by integer position.","Boolean mask: df[df['age'] > 30].","Combine conditions with & / | and parentheses.",".query() offers a string expression alternative."],
     "code":[("python","df.loc[df['city'] == 'NYC', ['name', 'age']]\ndf.iloc[0:2, :]                 # first two rows by position\ndf[(df['age'] > 25) & (df['city'] == 'NYC')]")],
     "example":{"title":"Assign to a filtered subset","text":"Use .loc to set values safely:","code":[("python","df.loc[df['age'] < 18, 'minor'] = True")]},
     "best":["Use .loc/.iloc explicitly; avoid chained indexing.","Wrap mask conditions in parentheses with & / |.","Use .loc for label-based assignment.","Prefer .query() for readable complex filters.","Reset the index after heavy filtering if needed."],
     "pitfalls":["Chained indexing (df[...][...] = ) causing SettingWithCopyWarning.","Using and/or instead of & / | on masks.","Off-by-one: .loc label ranges are inclusive, .iloc exclusive.","Mixing label and position selection.","Forgetting parentheses around conditions.","Assuming filtered frames are independent copies."],
     "interview":[(".loc vs .iloc?","loc selects by label (ranges inclusive); iloc by integer position (exclusive end)."),("How to filter rows?","Boolean mask: df[df.col > x], combine with & / | and parentheses."),("Why avoid chained indexing?","It may operate on a copy, so assignments silently don't persist (SettingWithCopyWarning)."),("How to assign to a subset safely?","Use df.loc[mask, col] = value."),("and/or vs & / |?","Use element-wise & / | with parentheses; Python and/or don't vectorize."),("What does .query() do?","Filters with a string expression, often more readable."),("Are loc ranges inclusive?","Yes for label-based loc; iloc end is exclusive."),("How to get a guaranteed copy?","Call .copy() on the selection.")],
     "practice":["Select rows by condition and specific columns with .loc.","Filter with a compound mask.","Fix a SettingWithCopyWarning using .loc."],
     "revision":".loc=label (inclusive), .iloc=position (exclusive), masks=filter (& / | + parens). Assign via .loc; avoid chained indexing; .query() for readability; .copy() to detach."},

    {"id":8,"slug":"08-groupby","name":"GroupBy: Split-Apply-Combine","category":"Pandas Analytics","level":"Intermediate",
     "summary":"Aggregate, transform, and filter by group.",
     "keywords":["groupby","aggregate","transform","split apply combine","agg"],"refs":["Group by"],
     "overview":"**GroupBy** implements split-apply-combine: split rows into groups by key(s), apply a function (aggregate/transform/filter), and combine results. It's the analytical workhorse for per-category metrics.",
     "concepts":["aggregate (agg) → one row per group.","transform → same shape as input (broadcast group stat back).","filter → keep/drop whole groups.","Group by multiple keys for hierarchical results.","Named aggregations for clean output columns."],
     "code":[("python","df.groupby('city').agg(\n    avg_age=('age', 'mean'),\n    n=('name', 'count'),\n).reset_index()")],
     "example":{"title":"Transform: group-relative value","text":"Subtract each group's mean (same shape back):","code":[("python","df['age_vs_city'] = df['age'] - df.groupby('city')['age'].transform('mean')")]},
     "best":["Use named aggregations for clear column names.","Use transform when you need the original shape back.","Group by multiple keys when needed.","Avoid apply with slow Python functions when agg/transform suffice.","reset_index() to flatten grouped output."],
     "pitfalls":["Confusing agg (collapses) with transform (same shape).","Slow groupby().apply(python_func) on big data.","Forgetting reset_index leaving a grouped index.","NaN keys silently dropped from groups.","Mutating groups in place unexpectedly.","Expecting order within groups without sorting."],
     "interview":[("Explain split-apply-combine.","Split rows into groups by key, apply a function per group, combine into a result."),("agg vs transform?","agg returns one row per group; transform returns a result aligned to the original rows."),("How to compute a group-relative metric?","groupby(key)[col].transform('mean') then subtract."),("What does filter do in groupby?","Keeps or removes entire groups based on a group-level condition."),("How to name aggregated columns?","Named aggregation: out_col=(in_col, func)."),("Why is apply slow?","It runs a Python function per group, missing vectorization."),("Are NaN group keys included?","By default they're excluded (dropna=True)."),("Multiple group keys?","Pass a list; you get a MultiIndex result.")],
     "practice":["Aggregate mean and count per category with named agg.","Add a group-relative column via transform.","Filter out groups smaller than N rows."],
     "revision":"GroupBy = split-apply-combine. agg collapses (one row/group), transform preserves shape, filter drops groups. Use named aggs, reset_index; avoid slow apply; NaN keys drop by default."},

    {"id":9,"slug":"09-merge-join","name":"Merge, Join & Concat","category":"Pandas Analytics","level":"Intermediate",
     "summary":"Combine DataFrames by keys or by stacking.",
     "keywords":["merge","join","concat","how","on","keys"],"refs":["Merge, join, concatenate"],
     "overview":"Combine tables with **merge** (SQL-style joins on keys), **join** (index-based), or **concat** (stack along an axis). The `how` parameter (inner/left/right/outer) controls which keys survive — mirroring SQL joins.",
     "concepts":["merge(on=...) joins on column keys; how sets the join type.","join() aligns on the index by default.","concat stacks rows (axis=0) or columns (axis=1).","Validate one-to-one/one-to-many to catch dup explosions.","indicator=True shows the match source."],
     "code":[("python","orders.merge(customers, on='customer_id', how='left', validate='m:1')")],
     "example":{"title":"Find unmatched rows","text":"Outer merge + indicator to detect mismatches:","code":[("python","m = a.merge(b, on='id', how='outer', indicator=True)\nonly_a = m[m['_merge'] == 'left_only']")]},
     "best":["Be explicit about how (inner/left/right/outer).","Use validate to assert cardinality (1:1, m:1).","Name/align join keys consistently.","Use indicator to debug joins.","concat with ignore_index when stacking unrelated frames."],
     "pitfalls":["Many-to-many merges exploding row counts.","Default inner join silently dropping rows.","Mismatched key dtypes failing to match.","Duplicate keys inflating results.","Index misalignment in concat producing NaNs.","Forgetting suffixes for overlapping column names."],
     "interview":[("merge vs join vs concat?","merge joins on keys (SQL-style), join aligns on index, concat stacks along an axis."),("What does how control?","Which keys are kept: inner (both), left, right, or outer (all)."),("How to detect unmatched rows?","Outer merge with indicator=True, then filter left_only/right_only."),("Why use validate?","To assert the join cardinality and catch unexpected duplicates/explosions."),("Cause of row explosion?","Many-to-many keys producing the Cartesian product per key."),("Why might a merge match nothing?","Key dtype mismatch or differing values/whitespace."),("Overlapping column names?","Disambiguate with suffixes."),("concat axis 0 vs 1?","0 stacks rows, 1 stacks columns (aligning on index).")],
     "practice":["Left-merge orders with customers using validate.","Use indicator to find unmatched keys.","Concatenate monthly frames with ignore_index."],
     "revision":"merge (keys, how=inner/left/right/outer), join (index), concat (stack rows/cols). Use validate + indicator; align key dtypes; beware m:m explosions and inner-join row loss."},

    {"id":10,"slug":"10-missing-data","name":"Missing Data & Cleaning","category":"Pandas Analytics","level":"Intermediate",
     "summary":"Detect, drop, fill, and impute NaNs cleanly.",
     "keywords":["nan","fillna","dropna","isna","imputation"],"refs":["Working with missing data"],
     "overview":"Real data has gaps. Pandas represents missing values as **NaN/NA**; clean them by detecting (`isna`), dropping (`dropna`), or filling/imputing (`fillna`, interpolation, group means). The right strategy depends on why data is missing.",
     "concepts":["isna/notna detect missing values.","dropna removes rows/cols with NaN (configurable thresholds).","fillna with constants, ffill/bfill, or computed values.","Group-wise imputation: fill with group mean/median.","Nullable dtypes (Int64, boolean) preserve NA in integers."],
     "code":[("python","df['age'] = df['age'].fillna(df['age'].median())\ndf = df.dropna(subset=['email'])           # require email\ndf['price'] = df['price'].ffill()          # carry last value forward")],
     "example":{"title":"Group-wise imputation","text":"Fill missing with the group's mean:","code":[("python","df['age'] = df['age'].fillna(df.groupby('city')['age'].transform('mean'))")]},
     "best":["Understand *why* values are missing before imputing.","Use median/group stats for robust imputation.","Drop only when missingness is small/uninformative.","Use nullable dtypes to keep NA in integer columns.","Document cleaning steps for reproducibility."],
     "pitfalls":["Blindly dropping rows and losing signal.","Filling with 0/mean when it biases analysis.","NaN != NaN comparisons (use isna).","ffill/bfill leaking future/past inappropriately.","Integer columns becoming float due to NaN.","Imputing before train/test split (data leakage)."],
     "interview":[("How are missing values represented?","As NaN (float NA) or pandas NA in nullable dtypes."),("How to detect missing values?","isna()/notna() (and .sum() to count per column)."),("Strategies to handle NaN?","Drop, fill with constants, ffill/bfill, or impute with statistics/models."),("Why median over mean for imputation?","Median is robust to outliers/skew."),("Why do int columns become float?","Classic NaN requires float; use nullable Int64 to keep integers."),("Risk of imputing before split?","Data leakage — fit imputation on train only."),("Group-wise imputation?","fillna with groupby(...).transform('mean'/'median')."),("ffill vs bfill caution?","They can leak temporally; ensure it's valid for the data.")],
     "practice":["Count missing values per column.","Impute a numeric column with its median.","Do group-wise mean imputation."],
     "revision":"Missing = NaN/NA: detect (isna), drop (dropna), fill/impute (fillna, ffill/bfill, group stats). Prefer median/group imputation, understand the cause, use nullable dtypes, avoid leakage."},

    {"id":11,"slug":"11-reshaping-pivot","name":"Reshaping: Pivot & Melt","category":"Pandas Analytics","level":"Intermediate",
     "summary":"Switch between wide and long formats.",
     "keywords":["pivot","melt","pivot_table","stack","wide long"],"refs":["Reshaping"],
     "overview":"Reshape between **long** (tidy) and **wide** formats: `pivot`/`pivot_table` spread a key column into columns; `melt` collapses columns into key/value rows; `stack`/`unstack` move between columns and index levels.",
     "concepts":["pivot_table aggregates (handles duplicates); pivot doesn't.","melt: wide → long (id_vars + value_vars).","stack/unstack move levels between index and columns.","Tidy (long) data is best for analysis; wide for display.","Aggfunc controls how pivot_table combines duplicates."],
     "code":[("python","wide = df.pivot_table(index='city', columns='year', values='sales', aggfunc='sum')\nlong = wide.reset_index().melt(id_vars='city', var_name='year', value_name='sales')")],
     "example":{"title":"Melt to tidy format","text":"Turn year columns into rows:","code":[("python","tidy = df.melt(id_vars=['name'], value_vars=['2025', '2026'],\n               var_name='year', value_name='amount')")]},
     "best":["Prefer tidy (long) data for analysis/groupby/plots.","Use pivot_table (not pivot) when duplicates exist.","Choose an explicit aggfunc.","Name var/value columns clearly in melt.","reset_index after pivot to flatten."],
     "pitfalls":["pivot failing on duplicate index/column pairs (use pivot_table).","Forgetting aggfunc and getting unexpected aggregation.","MultiIndex columns after pivot complicating access.","Losing columns not in id_vars during melt.","Confusing stack (cols→index) with unstack.","Wide format making row-wise analysis awkward."],
     "interview":[("pivot vs pivot_table?","pivot_table aggregates duplicates via aggfunc; pivot errors on duplicate index/column pairs."),("What does melt do?","Unpivots wide columns into long key/value rows (id_vars stay)."),("Wide vs long (tidy) data?","Long has one observation per row (best for analysis); wide spreads a variable across columns."),("stack vs unstack?","stack moves columns into the index (wider→longer); unstack does the reverse."),("Why specify aggfunc?","To define how duplicate cells combine (sum/mean/count)."),("Handling MultiIndex after pivot?","reset_index / flatten column levels for easier access."),("When pivot fails?","Duplicate (index, column) combinations — switch to pivot_table."),("Which format for groupby/plotting?","Long/tidy format.")],
     "practice":["Pivot sales by city × year with sum.","Melt year columns back to long format.","Use stack/unstack on a small frame."],
     "revision":"Reshape wide↔long: pivot_table (aggregates dup), melt (wide→long), stack/unstack (cols↔index). Prefer tidy/long for analysis; set aggfunc; flatten MultiIndex after pivot."},

    {"id":12,"slug":"12-performance","name":"Performance & Memory","category":"Performance","level":"Advanced",
     "summary":"Make Pandas fast and memory-lean at scale.",
     "keywords":["performance","vectorize","categorical","dtypes","chunk"],"refs":["Scaling to large datasets"],
     "overview":"Pandas slows down with row loops, object dtypes, and oversized types. Speed up by **vectorizing**, downcasting **dtypes**, using **categorical** for low-cardinality strings, reading in **chunks**, and reaching for Arrow-backed dtypes or Polars/Dask when data outgrows memory.",
     "concepts":["Avoid iterrows/apply; vectorize with column ops.","Downcast numeric dtypes (int64→int32, float64→float32).","category dtype slashes memory for repeated strings.","Read large files in chunks or select columns/dtypes.","Arrow-backed/Polars/Dask for very large data."],
     "code":[("python","df['city'] = df['city'].astype('category')        # memory win\ndf['count'] = pd.to_numeric(df['count'], downcast='integer')\nfor chunk in pd.read_csv('big.csv', chunksize=100_000):\n    process(chunk)")],
     "example":{"title":"Vectorize over apply","text":"Replace a row-wise apply with column math:","code":[("python","# slow: df.apply(lambda r: r.a + r.b, axis=1)\ndf['c'] = df['a'] + df['b']   # vectorized")]},
     "best":["Vectorize; avoid iterrows/axis=1 apply.","Use category dtype for low-cardinality strings.","Downcast numeric dtypes to cut memory.","Read only needed columns/dtypes; chunk big files.","Profile with %timeit / memory_usage before optimizing."],
     "pitfalls":["Row-wise apply/iterrows on large frames.","Object dtype strings wasting memory.","Loading entire huge CSVs into memory.","Over-downcasting causing overflow/precision loss.","Repeated concat in loops (quadratic).","Ignoring copy-vs-view memory implications."],
     "interview":[("Biggest Pandas speedups?","Vectorize (avoid loops/apply), right-size dtypes, use categoricals, and read selectively/in chunks."),("Why is apply(axis=1) slow?","It calls a Python function per row, bypassing vectorization."),("When use category dtype?","Low-cardinality repeated strings — large memory and groupby speedups."),("How to reduce memory?","Downcast numerics, categoricals, load fewer columns, Arrow-backed dtypes."),("Handling data bigger than RAM?","Chunked reads, Dask/Polars, or a database."),("Risk of downcasting?","Overflow or precision loss if the range/precision doesn't fit."),("Why avoid concat in loops?","Each concat copies — accumulate in a list and concat once."),("How to profile?","%timeit for speed, df.memory_usage(deep=True) for memory.")],
     "practice":["Convert a string column to category and measure memory.","Replace an apply(axis=1) with vectorized math.","Process a large CSV in chunks."],
     "revision":"Speed/memory: vectorize (no iterrows/apply axis=1), downcast dtypes, category for repeated strings, chunked/selective reads, Polars/Dask/Arrow for big data. Profile first; concat once, not in loops."},
  ],
}
