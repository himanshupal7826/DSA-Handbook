# Go Handbook spec
SPEC = {
  "id": "go", "name": "Go Handbook", "icon": "🐹",
  "tagline": "Master Go for backend and systems interviews: syntax, goroutines, channels, interfaces, error handling, and idiomatic concurrency.",
  "itemNoun": "Topic",
  "levels": ["Beginner", "Intermediate", "Advanced"],
  "categories": [
    {"name": "Language Basics", "level": "Beginner", "icon": "📘"},
    {"name": "Types & Interfaces", "level": "Beginner", "icon": "🧱"},
    {"name": "Concurrency", "level": "Intermediate", "icon": "🔀"},
    {"name": "Idioms & Tooling", "level": "Intermediate", "icon": "🛠️"},
    {"name": "Runtime & Performance", "level": "Advanced", "icon": "⚡"},
  ],
  "items": [
    {"id":1,"slug":"01-basics-syntax","name":"Syntax, Variables & Functions","category":"Language Basics","level":"Beginner",
     "summary":"Go's minimal syntax: packages, typed variables, multiple returns.",
     "keywords":["var","func","package","short declaration",":="],"refs":["A Tour of Go"],
     "overview":"Go is a statically-typed, compiled language with a deliberately small syntax. Programs are organized into **packages**; `main` is the entry point. Functions can return multiple values, which underpins Go's error-handling idiom.",
     "concepts":["`:=` declares + infers type inside functions; `var` works anywhere.","Functions return multiple values: `val, err := f()`.","Exported identifiers start with an uppercase letter.","Zero values: 0, \"\", nil, false — no uninitialized variables.","No implicit type conversion; convert explicitly."],
     "code":[("go","package main\n\nimport \"fmt\"\n\n// divide returns a result and an error (idiomatic multi-return).\nfunc divide(a, b int) (int, error) {\n    if b == 0 {\n        return 0, fmt.Errorf(\"divide by zero\")\n    }\n    return a / b, nil\n}\n\nfunc main() {\n    q, err := divide(10, 2)\n    if err != nil {\n        fmt.Println(\"error:\", err)\n        return\n    }\n    fmt.Println(\"quotient:\", q) // 5\n}")],
     "example":{"title":"Named returns + defer","text":"Named returns can be set in a deferred function:","code":[("go","func read() (n int, err error) {\n    defer func() { if err != nil { n = 0 } }()\n    // ...\n    return 42, nil\n}")]},
     "best":["Handle every returned error explicitly.","Prefer `:=` inside functions; `var` for package-level/zero-value clarity.","Keep functions small and return early.","Use gofmt — formatting is non-negotiable in Go.","Group related code into packages by responsibility."],
     "pitfalls":["Ignoring errors with `_` when they matter.","Shadowing variables with `:=` in inner scopes.","Unused imports/variables are compile errors.","Assuming zero value is 'unset' (it's a real value).","Mixing tabs/spaces — let gofmt handle it.","Forgetting that Go has no exceptions for normal control flow."],
     "interview":[("How does Go handle errors?","Functions return an error value; callers check `if err != nil`. No exceptions for expected failures."),("`:=` vs `var`?","`:=` declares with inferred type inside functions; `var` works at package scope and when you want the zero value or explicit type."),("What are zero values?","Default initialized values: 0, false, \"\", nil — every variable is always initialized."),("How are identifiers exported?","Capitalized names are exported (public) across packages; lowercase are package-private."),("Does Go allow implicit conversions?","No — you must convert types explicitly, e.g., float64(i)."),("What is defer?","Schedules a call to run when the surrounding function returns, used for cleanup (closing files, unlocking)."),("Why are unused variables errors?","Go enforces clean code at compile time to prevent bugs and dead code."),("Multiple return values use?","Returning (result, error) or (value, ok) pairs idiomatically.")],
     "practice":["Write a function returning (value, error) and handle it.","Use defer to close a resource.","Demonstrate variable shadowing and fix it."],
     "revision":"Go: small syntax, packages, multi-return for errors, zero values, explicit conversions, gofmt-enforced style. Handle every error; defer for cleanup."},

    {"id":2,"slug":"02-slices-maps","name":"Slices, Arrays & Maps","category":"Language Basics","level":"Beginner",
     "summary":"Go's core collections and their sharing semantics.",
     "keywords":["slice","array","map","append","make","len cap"],"refs":["Go blog: Slices"],
     "overview":"Arrays are fixed-size; **slices** are growable views over an underlying array (the workhorse). **Maps** are hash tables. Slices share backing storage, so aliasing and `append` growth are common sources of subtle bugs.",
     "concepts":["A slice has pointer, length, and capacity; reslicing shares the array.","`append` may reallocate when capacity is exceeded, breaking aliases.","Maps are unordered; iteration order is randomized.","`make([]T, len, cap)` preallocates; nil slices/maps behave specially.","Reading a missing map key returns the zero value + ok=false."],
     "code":[("go","s := make([]int, 0, 4)   // len 0, cap 4\ns = append(s, 1, 2, 3)\n\nm := map[string]int{\"a\": 1}\nv, ok := m[\"b\"] // v=0, ok=false\nfmt.Println(v, ok)\n\nfor k, val := range m { // order is random\n    fmt.Println(k, val)\n}")],
     "example":{"title":"append aliasing gotcha","text":"Two slices may share memory until a reallocation:","code":[("go","a := []int{1, 2, 3}\nb := a[:2]\nb = append(b, 99) // overwrites a[2] if cap allows!\nfmt.Println(a)     // [1 2 99]")]},
     "best":["Preallocate with make(..., 0, n) when size is known.","Copy slices with `copy` when you need independence.","Check map presence with the comma-ok form.","Don't rely on map iteration order.","Return a fresh slice from functions to avoid aliasing surprises."],
     "pitfalls":["append reallocating and silently de-aliasing (or not).","Assuming map iteration order is stable.","Writing to a nil map panics (reads are fine).","Holding a small slice of a huge array keeping it all in memory.","Off-by-one in slice expressions a[low:high].","Comparing slices/maps with == (not allowed; only nil compare)."],
     "interview":[("Slice vs array?","Array is fixed-size value; slice is a growable reference (pointer+len+cap) over an array."),("What does append do on full capacity?","Allocates a larger backing array and copies, so previous aliases may diverge."),("Why randomize map iteration?","To prevent code from depending on order, which isn't guaranteed."),("comma-ok idiom?","`v, ok := m[k]` distinguishes a present zero value from a missing key."),("Can you write to a nil map?","No — it panics; you must make() it first. Reads return zero values."),("How to copy a slice safely?","Allocate a new slice and use copy(dst, src)."),("Memory leak from slicing?","A small sub-slice keeps the whole backing array alive; copy to release it."),("Are slices comparable?","Not with ==; only comparison to nil is allowed.")],
     "practice":["Demonstrate an append aliasing bug and fix with copy.","Use comma-ok to handle missing keys.","Preallocate a slice for known size and benchmark."],
     "revision":"Slices = pointer+len+cap views (share + may realloc on append); maps = unordered hash tables (comma-ok, nil-write panics). Preallocate, copy for independence, don't depend on order."},

    {"id":3,"slug":"03-structs-methods","name":"Structs & Methods","category":"Types & Interfaces","level":"Beginner",
     "summary":"Compose data with structs; attach behavior with methods.",
     "keywords":["struct","method","receiver","pointer receiver","embedding"],"refs":["Effective Go"],
     "overview":"Structs group fields; methods attach behavior to types via a **receiver**. Go favors **composition via embedding** over inheritance. Pointer vs value receivers determine whether methods can mutate and affect copying cost.",
     "concepts":["Pointer receiver `(*T)` can mutate and avoids copying; value receiver `(T)` operates on a copy.","Embed a type to promote its fields/methods (composition).","Struct values are copied on assignment and function calls.","Zero-value structs are usable if designed for it."],
     "code":[("go","type Counter struct{ n int }\n\n// pointer receiver: mutates the original\nfunc (c *Counter) Inc() { c.n++ }\n\n// value receiver: read-only view\nfunc (c Counter) Value() int { return c.n }\n\nfunc main() {\n    c := Counter{}\n    c.Inc(); c.Inc()\n    fmt.Println(c.Value()) // 2\n}")],
     "example":{"title":"Embedding for composition","text":"Promote methods from an embedded type:","code":[("go","type Logger struct{ prefix string }\nfunc (l Logger) Log(s string) { fmt.Println(l.prefix, s) }\n\ntype Service struct {\n    Logger // embedded\n    name string\n}\n// s.Log(...) works via promotion")]},
     "best":["Use pointer receivers when mutating or for large structs; be consistent per type.","Prefer composition (embedding) over deep type hierarchies.","Design zero values to be useful (e.g., bytes.Buffer).","Keep structs focused; avoid giant god-structs.","Document exported struct fields."],
     "pitfalls":["Mixing value and pointer receivers on the same type causing method-set confusion.","Mutating a value receiver expecting it to persist (it won't).","Copying a struct that contains a mutex (copies the lock).","Embedding ambiguity when two embedded types share a method name.","Large struct value copies hurting performance.","Exported fields breaking invariants without validation."],
     "interview":[("Pointer vs value receiver?","Pointer can mutate and avoids copying; value works on a copy. Be consistent within a type."),("How does Go do inheritance?","It doesn't — it uses composition via embedding, promoting fields and methods."),("What is a method set?","The set of methods callable on a type/pointer; pointer receivers are only in the pointer's method set."),("Why not copy a struct with a mutex?","Copying duplicates lock state, breaking mutual exclusion; pass by pointer."),("Embedding vs subclassing?","Embedding promotes members but has no virtual dispatch/override; it's composition."),("When are structs copied?","On assignment, function arguments, and range over a slice of structs."),("Zero-value usability?","Designing types so the zero value works without initialization (idiomatic)."),("Field tags?","Struct tags (e.g., `json:\"name\"`) provide metadata for reflection-based libraries.")],
     "practice":["Add Inc/Value methods with correct receivers.","Embed a Logger into a Service.","Show a value-receiver mutation bug and fix it."],
     "revision":"Structs group data; methods attach behavior via receivers (pointer=mutate/cheap, value=copy). Compose via embedding, not inheritance; don't copy structs holding mutexes."},

    {"id":4,"slug":"04-interfaces","name":"Interfaces","category":"Types & Interfaces","level":"Intermediate",
     "summary":"Implicit, duck-typed interfaces for decoupling.",
     "keywords":["interface","implicit","polymorphism","type assertion","empty interface"],"refs":["Effective Go: Interfaces"],
     "overview":"A Go interface is a set of method signatures. Types satisfy interfaces **implicitly** — no `implements` keyword. This enables loose coupling and testability. `any` (empty interface) holds any value; type assertions/switches recover the concrete type.",
     "concepts":["Implicit satisfaction: implement the methods and you satisfy the interface.","Accept interfaces, return concrete types (a common guideline).","An interface value is (type, value); nil interface vs nil pointer differ.","Type assertion `v, ok := x.(T)`; type switch for many cases.","Keep interfaces small (often one method)."],
     "code":[("go","type Stringer interface{ String() string }\n\ntype Point struct{ X, Y int }\nfunc (p Point) String() string { return fmt.Sprintf(\"(%d,%d)\", p.X, p.Y) }\n\nfunc print(s Stringer) { fmt.Println(s.String()) }\n// Point satisfies Stringer implicitly")],
     "example":{"title":"Type switch","text":"Branch on the dynamic type held by an interface:","code":[("go","func describe(x any) string {\n    switch v := x.(type) {\n    case int:    return fmt.Sprintf(\"int %d\", v)\n    case string: return \"string \" + v\n    default:     return \"unknown\"\n    }\n}")]},
     "best":["Keep interfaces small (io.Reader/Writer style).","Define interfaces where they're consumed, not where implemented.","Accept interfaces as parameters for flexibility/testing.","Use type switches sparingly; prefer polymorphic methods.","Return concrete types so callers retain full API."],
     "pitfalls":["The nil-interface trap: a nil *T stored in an interface is non-nil.","Huge interfaces that are hard to implement/mock.","Overusing `any` and losing type safety.","Type-asserting without the comma-ok and panicking.","Defining interfaces prematurely (speculative abstraction).","Comparing interface values holding uncomparable types (panics)."],
     "interview":[("How are interfaces satisfied in Go?","Implicitly — a type that has the required methods satisfies the interface, no declaration needed."),("The nil interface gotcha?","An interface holding a nil pointer has a non-nil type, so `iface != nil` is true unexpectedly."),("Why keep interfaces small?","Small interfaces are easy to implement, mock, and compose (e.g., io.Reader)."),("Type assertion vs type switch?","Assertion extracts one concrete type (with ok); switch branches across many types."),("'Accept interfaces, return structs' — why?","Flexible inputs for callers/tests, concrete outputs preserve the full API."),("What is the empty interface?","`any`/`interface{}` holds any value; recover the concrete type via assertion/switch."),("Where to define interfaces?","At the consumer, so packages depend on behavior they need, not implementations."),("How does an interface value compare?","Equal if both type and value are equal; panics if the dynamic type is uncomparable.")],
     "practice":["Implement io.Writer for a custom buffer.","Use a type switch to handle multiple input types.","Trigger and explain the nil-interface trap."],
     "revision":"Interfaces = implicit method sets for decoupling. Keep them small, define at the consumer, accept interfaces/return concretes. Mind the nil-interface trap and prefer comma-ok assertions."},

    {"id":5,"slug":"05-goroutines","name":"Goroutines","category":"Concurrency","level":"Intermediate",
     "summary":"Lightweight concurrent functions multiplexed onto OS threads.",
     "keywords":["goroutine","go keyword","scheduler","concurrency","waitgroup"],"refs":["Go blog: Concurrency"],
     "overview":"A goroutine is a function running concurrently, scheduled by the Go runtime onto a small pool of OS threads. They're cheap (KBs of stack) so you can run thousands. Coordinate completion with `sync.WaitGroup`; never assume ordering.",
     "concepts":["`go f()` starts a goroutine; it returns immediately.","Goroutines are multiplexed (M:N) onto OS threads by the scheduler.","Use sync.WaitGroup to wait for a group to finish.","The main goroutine exiting kills all others.","Concurrency is not parallelism — GOMAXPROCS controls parallelism."],
     "code":[("go","var wg sync.WaitGroup\nfor i := 0; i < 3; i++ {\n    wg.Add(1)\n    go func(id int) {       // pass i as arg to avoid capture bug\n        defer wg.Done()\n        fmt.Println(\"worker\", id)\n    }(i)\n}\nwg.Wait() // block until all done")],
     "example":{"title":"Loop variable capture (pre-Go 1.22)","text":"Capturing the loop variable by reference is a classic bug:","code":[("go","// BUG (old Go): all goroutines may print the final i\nfor i := 0; i < 3; i++ { go func(){ fmt.Println(i) }() }\n// FIX: pass it in -> go func(i int){...}(i)")]},
     "best":["Pass loop variables as arguments to goroutines.","Always have a way to wait for or cancel goroutines.","Avoid leaking goroutines blocked forever on a channel.","Use the race detector (go test -race).","Limit concurrency with a worker pool / semaphore."],
     "pitfalls":["Loop-variable capture launching goroutines sharing one variable.","Goroutine leaks blocked on channels with no sender/receiver.","Main exiting before goroutines run.","Data races on shared variables (use channels or mutexes).","Unbounded goroutine creation exhausting memory.","Forgetting wg.Add before launching."],
     "interview":[("Goroutine vs OS thread?","Goroutines are user-space, cheap (~KB stack), multiplexed M:N onto threads by the runtime scheduler."),("Concurrency vs parallelism?","Concurrency is structuring independent tasks; parallelism is running them simultaneously (needs multiple cores/GOMAXPROCS)."),("How to wait for goroutines?","sync.WaitGroup: Add before launch, Done on finish, Wait to block."),("Classic loop-capture bug?","Goroutines closing over the loop variable share it; pass it as a parameter (fixed by Go 1.22 per-iteration scoping)."),("What is a goroutine leak?","A goroutine blocked forever (e.g., on a channel) that never exits, wasting memory."),("How to detect data races?","Run with -race; it instruments memory access to flag concurrent unsynchronized access."),("How to bound concurrency?","Worker pool or a buffered-channel semaphore."),("What kills goroutines?","Nothing explicitly — they end when their function returns or the program exits.")],
     "practice":["Launch N workers with a WaitGroup.","Reproduce and fix the loop-capture bug.","Run a program under -race and fix a reported race."],
     "revision":"Goroutines = cheap concurrent funcs (M:N scheduled). Coordinate with WaitGroup; pass loop vars as args; avoid leaks/races; bound with pools. Concurrency ≠ parallelism."},

    {"id":6,"slug":"06-channels","name":"Channels & select","category":"Concurrency","level":"Intermediate",
     "summary":"Typed pipes for goroutine communication and synchronization.",
     "keywords":["channel","select","buffered","close","deadlock"],"refs":["Go blog: Share memory by communicating"],
     "overview":"Channels are typed conduits that let goroutines communicate by passing values — Go's motto is *'share memory by communicating.'* Unbuffered channels synchronize sender and receiver; buffered channels decouple them. `select` waits on multiple channel operations.",
     "concepts":["Send `ch <- v`, receive `v := <-ch`. Unbuffered = rendezvous.","Buffered channel holds N values before blocking the sender.","Close signals 'no more values'; receivers get zero value + ok=false.","`select` picks a ready case; `default` makes it non-blocking.","Only the sender should close a channel; never close twice."],
     "code":[("go","ch := make(chan int)      // unbuffered\ngo func() { ch <- 42 }()  // blocks until received\nv := <-ch                 // receives 42\n\n// fan-in with select + timeout\nselect {\ncase x := <-ch:\n    fmt.Println(x)\ncase <-time.After(time.Second):\n    fmt.Println(\"timeout\")\n}")],
     "example":{"title":"Worker pool","text":"Distribute jobs over channels:","code":[("go","jobs := make(chan int, 100)\nresults := make(chan int, 100)\nfor w := 0; w < 3; w++ {\n    go func(){ for j := range jobs { results <- j*j } }()\n}\nfor i := 1; i <= 5; i++ { jobs <- i }\nclose(jobs)")]},
     "best":["Let the sender own and close the channel.","Use buffered channels to decouple producer/consumer rates.","Use select with context for cancellation/timeouts.","Range over a channel to drain until closed.","Prefer channels for ownership transfer, mutexes for shared state."],
     "pitfalls":["Deadlock: sending on an unbuffered channel with no receiver.","Closing a channel twice or sending on a closed channel (panic).","Receiving from a nil channel blocks forever.","Goroutine leak when nobody drains a channel.","Assuming buffered = unlimited (it blocks when full).","Using channels where a simple mutex is clearer."],
     "interview":[("Unbuffered vs buffered channel?","Unbuffered synchronizes sender/receiver (rendezvous); buffered allows N queued values before blocking."),("Who should close a channel?","The sender, exactly once; closing signals completion to receivers."),("What does select do?","Blocks until one of several channel operations is ready; default makes it non-blocking."),("How to implement a timeout?","select with a case on time.After (or a context's Done channel)."),("What causes a channel deadlock?","All goroutines blocked, e.g., sending on an unbuffered channel with no receiver."),("Receiving from a closed channel?","Returns the zero value immediately with ok=false."),("Channels vs mutexes — when?","Channels to transfer ownership/coordinate; mutexes to protect shared mutable state."),("Nil channel behavior?","Send/receive on a nil channel blocks forever — useful to disable a select case.")],
     "practice":["Build a 3-worker pool over jobs/results channels.","Add a timeout with select + time.After.","Trigger and fix an unbuffered-channel deadlock."],
     "revision":"Channels pass typed values between goroutines (unbuffered=rendezvous, buffered=decouple). select multiplexes; sender closes once. Deadlocks come from no-receiver sends and nil channels."},

    {"id":7,"slug":"07-context","name":"Context: Cancellation & Deadlines","category":"Concurrency","level":"Advanced",
     "summary":"Propagate cancellation, deadlines, and request values.",
     "keywords":["context","cancel","deadline","timeout","WithCancel"],"refs":["Go blog: Context"],
     "overview":"`context.Context` carries cancellation signals, deadlines, and request-scoped values across API boundaries and goroutines. It's the standard way to stop work (HTTP requests, DB queries) when the caller gives up.",
     "concepts":["Pass ctx as the first parameter: `func F(ctx context.Context, ...)`.","WithCancel/WithTimeout/WithDeadline derive child contexts.","Select on `ctx.Done()` to abort blocking work.","Always call the cancel function (defer cancel()).","Don't store contexts in structs; pass them explicitly."],
     "code":[("go","ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)\ndefer cancel()\n\nselect {\ncase res := <-doWork(ctx):\n    fmt.Println(res)\ncase <-ctx.Done():\n    fmt.Println(\"aborted:\", ctx.Err()) // deadline exceeded / canceled\n}")],
     "example":{"title":"Propagating cancellation","text":"Downstream calls inherit the deadline:","code":[("go","func handler(ctx context.Context) error {\n    rows, err := db.QueryContext(ctx, \"SELECT ...\") // canceled if ctx is\n    _ = rows\n    return err\n}")]},
     "best":["Thread ctx through all blocking/IO calls.","Always defer cancel() to release resources.","Use context.Background() at the top; derive children below.","Check ctx.Err() to know why work stopped.","Keep request-scoped values minimal and typed via keys."],
     "pitfalls":["Forgetting defer cancel() → context/timer leak.","Storing context in a struct field.","Ignoring ctx.Done() in long loops (no cancellation).","Putting large/optional data in context values.","Passing nil context (use Background/TODO).","Assuming cancellation is instant — code must check it."],
     "interview":[("What is context used for?","Propagating cancellation, deadlines/timeouts, and request-scoped values across goroutines and APIs."),("Why defer cancel()?","To release the context's resources/timer even on the happy path; not calling it leaks."),("How does a goroutine observe cancellation?","By selecting on ctx.Done() and returning when it closes."),("WithTimeout vs WithDeadline?","Timeout is relative duration; deadline is an absolute time — both auto-cancel."),("Where should ctx go in a signature?","First parameter, conventionally named ctx."),("Should context carry business data?","No — only request-scoped values like auth/trace IDs, kept small."),("What does ctx.Err() return?","nil if active, else context.Canceled or context.DeadlineExceeded."),("Background vs TODO?","Background is the root for real use; TODO marks unfinished plumbing.")],
     "practice":["Add a 2s timeout around a blocking call.","Propagate ctx into a DB/HTTP call.","Make a worker loop honor ctx.Done()."],
     "revision":"Context propagates cancel/deadline/values; pass as first arg, defer cancel(), select on Done(). Don't store in structs or stuff business data in it."},

    {"id":8,"slug":"08-error-handling","name":"Error Handling & Wrapping","category":"Idioms & Tooling","level":"Intermediate",
     "summary":"Explicit errors, wrapping, and sentinel/typed errors.",
     "keywords":["error","errors.Is","errors.As","wrap","%w","panic recover"],"refs":["Go blog: Error handling"],
     "overview":"Go treats errors as values. Wrap errors with `%w` to add context while preserving the chain; inspect with `errors.Is` (sentinel match) and `errors.As` (typed extraction). `panic`/`recover` are for truly exceptional, unrecoverable situations — not control flow.",
     "concepts":["Return errors; don't throw. Check `if err != nil`.","Wrap with fmt.Errorf(\"...: %w\", err) to keep the cause.","errors.Is compares against a sentinel; errors.As extracts a type.","Sentinel errors (var ErrNotFound = errors.New(...)) for known cases.","panic/recover only at boundaries (e.g., top of a goroutine)."],
     "code":[("go","var ErrNotFound = errors.New(\"not found\")\n\nfunc get(id int) (User, error) {\n    u, ok := store[id]\n    if !ok {\n        return User{}, fmt.Errorf(\"get user %d: %w\", id, ErrNotFound)\n    }\n    return u, nil\n}\n\n// caller\nif errors.Is(err, ErrNotFound) { /* handle 404 */ }")],
     "example":{"title":"Typed error extraction","text":"errors.As pulls out a concrete error type:","code":[("go","var perr *os.PathError\nif errors.As(err, &perr) {\n    fmt.Println(\"path:\", perr.Path)\n}")]},
     "best":["Wrap with %w to add context at each layer.","Define sentinel/typed errors for cases callers branch on.","Inspect with errors.Is/As, not string matching.","Don't log and return the same error repeatedly.","Reserve panic for programmer errors / unrecoverable states."],
     "pitfalls":["Comparing errors with == after wrapping (use errors.Is).","String-matching error messages.","Swallowing errors with _.","Wrapping without context (just returning err loses the call site value-add).","Using panic for normal control flow.","recover() in the wrong goroutine (it only catches its own)."],
     "interview":[("How does Go signal errors?","As ordinary return values checked with if err != nil — no exceptions for expected failures."),("What does %w do?","Wraps an error preserving the chain so errors.Is/As can inspect the cause."),("errors.Is vs errors.As?","Is checks equality against a sentinel through the chain; As extracts a matching concrete type."),("When use panic/recover?","Only for unrecoverable/programmer errors or at goroutine/server boundaries to avoid crashing the process."),("Sentinel error pattern?","Exported `var ErrX = errors.New(...)` that callers match with errors.Is."),("Why not match error strings?","Brittle; wrapping/i18n changes them. Use typed/sentinel checks."),("Does recover work across goroutines?","No — recover only catches panics in its own goroutine."),("How to add context without losing the cause?","fmt.Errorf with %w including identifiers like ids/operations.")],
     "practice":["Wrap an error with %w and match it via errors.Is.","Extract a typed error with errors.As.","Add a recover at the top of a goroutine."],
     "revision":"Errors are values: return + check, wrap with %w, inspect with errors.Is/As, define sentinels/typed errors. panic/recover only for the truly exceptional, per-goroutine."},

    {"id":9,"slug":"09-generics","name":"Generics","category":"Idioms & Tooling","level":"Advanced",
     "summary":"Type parameters for reusable, type-safe code (Go 1.18+).",
     "keywords":["generics","type parameters","constraints","comparable","any"],"refs":["Go generics tutorial"],
     "overview":"Generics let functions and types operate over a set of types via **type parameters** with **constraints**. They replace many uses of `interface{}` + reflection with compile-time type safety, useful for containers and algorithms.",
     "concepts":["`func Map[T, U any](s []T, f func(T) U) []U` — type params in brackets.","Constraints limit allowed types (e.g., comparable, constraints.Ordered).","The compiler infers type args from arguments when possible.","Use generics for data structures/algorithms, not everything.","Constraints are interfaces (method or type sets)."],
     "code":[("go","func Map[T, U any](s []T, f func(T) U) []U {\n    out := make([]U, len(s))\n    for i, v := range s {\n        out[i] = f(v)\n    }\n    return out\n}\n\ndoubled := Map([]int{1, 2, 3}, func(x int) int { return x * 2 })")],
     "example":{"title":"Ordered constraint","text":"A generic Max using a type-set constraint:","code":[("go","type Ordered interface { ~int | ~int64 | ~float64 | ~string }\nfunc Max[T Ordered](a, b T) T { if a > b { return a }; return b }")]},
     "best":["Use generics for collections/algorithms with repeated logic.","Pick the narrowest constraint that works.","Let type inference reduce verbosity.","Prefer concrete types when only one type is used.","Combine with interfaces when behavior (methods) is needed."],
     "pitfalls":["Over-engineering simple code with type parameters.","Constraints too broad (any) losing needed operations.","Confusing method-based vs type-set constraints.","Expecting generic specialization to always speed things up.","Forgetting ~ for underlying-type constraints.","Generic code that's harder to read than a small duplication."],
     "interview":[("What problem do generics solve?","Type-safe reuse over many types without interface{}+reflection or copy-paste."),("What is a constraint?","An interface limiting acceptable type arguments (methods and/or a type set)."),("comparable constraint?","Permits == and map keys; used for generic sets/dedup."),("When NOT to use generics?","When a single concrete type suffices or an interface models the behavior better."),("How does inference help?","The compiler deduces type arguments from the call's argument types."),("What does ~int mean in a constraint?","Any type whose underlying type is int (includes named types)."),("Generics vs interfaces?","Generics give compile-time type identity/reuse; interfaces give runtime polymorphism over behavior."),("Performance of generics?","Generally good; the compiler may share instantiations (gcshape) — measure if critical.")],
     "practice":["Write a generic Filter[T].","Implement Max with an Ordered constraint.","Build a generic Set[T comparable]."],
     "revision":"Generics = type parameters + constraints for type-safe reuse (containers/algorithms). Use narrow constraints, lean on inference, and don't over-abstract simple code."},

    {"id":10,"slug":"10-concurrency-patterns","name":"Concurrency Patterns & Sync","category":"Runtime & Performance","level":"Advanced",
     "summary":"Mutexes, atomics, and pipeline/fan-out patterns.",
     "keywords":["mutex","sync","atomic","race","pipeline","fan-out"],"refs":["sync package; race detector"],
     "overview":"When goroutines share memory, protect it with `sync.Mutex`/`RWMutex` or use `sync/atomic` for simple counters. Structured patterns — pipelines, fan-out/fan-in, worker pools — keep concurrent code correct and bounded. Always test with the race detector.",
     "concepts":["Mutex guards a critical section; RWMutex allows concurrent readers.","sync/atomic for lock-free counters/flags.","sync.Once for one-time initialization.","Pipeline: stages connected by channels; fan-out: many workers from one channel.","Detect races with go test/build -race."],
     "code":[("go","type SafeCounter struct {\n    mu sync.Mutex\n    n  int\n}\nfunc (c *SafeCounter) Inc() {\n    c.mu.Lock()\n    defer c.mu.Unlock()\n    c.n++\n}")],
     "example":{"title":"Fan-out / fan-in","text":"Spread work, then merge results:","code":[("go","func fanIn(chs ...<-chan int) <-chan int {\n    out := make(chan int)\n    var wg sync.WaitGroup\n    for _, c := range chs {\n        wg.Add(1)\n        go func(c <-chan int){ defer wg.Done(); for v := range c { out <- v } }(c)\n    }\n    go func(){ wg.Wait(); close(out) }()\n    return out\n}")]},
     "best":["Guard all access to shared mutable state with the same lock.","Keep critical sections short.","Use atomics for simple counters/flags instead of a mutex.","Bound concurrency with worker pools.","Run -race in CI."],
     "pitfalls":["Forgetting to unlock (use defer).","Copying a struct containing a Mutex.","Mixed atomic and non-atomic access to the same variable.","Deadlocks from acquiring locks in inconsistent order.","RWMutex misuse (writers starved or read-locked during writes).","Closing a channel from multiple goroutines."],
     "interview":[("Mutex vs channel?","Mutex protects shared state in place; channels transfer ownership/coordinate. Choose by the problem."),("When use sync/atomic?","For simple lock-free counters/flags where a full mutex is overkill."),("What does sync.Once do?","Guarantees a function runs exactly once, e.g., lazy singleton init."),("RWMutex benefit?","Multiple concurrent readers, exclusive writers — good for read-heavy data."),("How to bound concurrency?","Worker pool or a semaphore channel of fixed size."),("Why not copy a Mutex?","Copying duplicates lock state and breaks mutual exclusion."),("Fan-out/fan-in?","Distribute work to N workers (fan-out) and merge their outputs into one channel (fan-in)."),("How to find races?","Build/test with -race; it instruments memory accesses to detect unsynchronized sharing.")],
     "practice":["Make a thread-safe counter with Mutex and with atomic.","Build a pipeline of 3 stages via channels.","Implement fan-out/fan-in for a CPU task."],
     "revision":"Protect shared state with Mutex/RWMutex or atomics; structure work as pipelines/fan-out/worker pools; keep critical sections short; never copy a Mutex; test with -race."},
  ],
}
