# Kubernetes Handbook spec
SPEC = {
  "id": "kubernetes", "name": "Kubernetes Handbook", "icon": "☸️",
  "tagline": "Master Kubernetes for SRE/backend interviews: pods, deployments, services, config, scaling, and troubleshooting.",
  "itemNoun": "Topic",
  "levels": ["Beginner", "Intermediate", "Advanced"],
  "categories": [
    {"name": "Core Objects", "level": "Beginner", "icon": "🧩"},
    {"name": "Networking", "level": "Intermediate", "icon": "🌐"},
    {"name": "Config & State", "level": "Intermediate", "icon": "⚙️"},
    {"name": "Scaling & Scheduling", "level": "Advanced", "icon": "📈"},
    {"name": "Operations", "level": "Advanced", "icon": "🛠️"},
  ],
  "items": [
    {"id":1,"slug":"01-architecture","name":"Kubernetes Architecture","category":"Core Objects","level":"Beginner",
     "summary":"Control plane + worker nodes reconciling desired state.",
     "keywords":["control plane","node","kubelet","api server","etcd","scheduler"],"refs":["K8s components"],
     "overview":"Kubernetes is a **declarative** container orchestrator. You submit desired state to the **API server**; controllers continuously **reconcile** actual toward desired. The **control plane** (API server, etcd, scheduler, controller-manager) decides; **worker nodes** (kubelet, kube-proxy, runtime) execute.",
     "concepts":["API server is the single front door; state stored in etcd.","Scheduler assigns pods to nodes by resources/constraints.","Controllers reconcile desired vs actual state in a loop.","kubelet runs pods on each node; kube-proxy handles service routing.","Everything is a declarative API object."],
     "code":[("bash","kubectl get nodes\nkubectl get pods -A          # all namespaces\nkubectl cluster-info")],
     "example":{"title":"Reconciliation loop","text":"Declare 3 replicas; if one dies, a controller recreates it to match desired state.","code":[("bash","kubectl scale deployment web --replicas=3\nkubectl get deploy web -w      # watch it converge")]},
     "best":["Think declaratively: describe desired state, let controllers converge.","Keep manifests in version control (GitOps).","Use namespaces to isolate environments/teams.","Don't write to etcd directly — go through the API.","Label everything for selection and ops."],
     "pitfalls":["Treating kubectl as imperative scripting in prod.","Editing live objects instead of updating manifests.","Ignoring the difference between desired and observed state.","Single control-plane node (no HA).","Storing secrets unencrypted in etcd.","Confusing nodes (machines) with pods (workloads)."],
     "interview":[("What does the control plane do?","Stores desired state (etcd via API server) and reconciles it: scheduling and controllers drive actual toward desired."),("What is reconciliation?","Controllers continuously compare desired vs actual state and act to close the gap."),("Role of the scheduler?","Places pending pods onto suitable nodes based on resources, affinity, taints."),("kubelet vs kube-proxy?","kubelet runs/monitors pods on a node; kube-proxy programs service networking rules."),("Why is etcd critical?","It's the cluster's source of truth (all state); back it up and secure it."),("Imperative vs declarative?","Declarative manifests describe end state and are reconciled; imperative commands take direct actions."),("What is a namespace?","A virtual cluster scope for isolating and organizing objects."),("Node vs pod?","A node is a worker machine; a pod is the smallest deployable unit running containers.")],
     "practice":["List nodes and pods across namespaces.","Scale a deployment and watch reconciliation.","Explain the path of a kubectl apply request."],
     "revision":"K8s = declarative orchestrator. Control plane (API/etcd/scheduler/controllers) reconciles desired state; nodes (kubelet/kube-proxy/runtime) execute. Manifests in git, label everything, secure etcd."},

    {"id":2,"slug":"02-pods","name":"Pods","category":"Core Objects","level":"Beginner",
     "summary":"The smallest deployable unit — one or more co-located containers.",
     "keywords":["pod","container","sidecar","init container","ephemeral"],"refs":["Pods"],
     "overview":"A **Pod** wraps one or more containers that share a network namespace (same IP/port space) and storage. Pods are **ephemeral** — you rarely create them directly; controllers (Deployments) manage their lifecycle and replacement.",
     "concepts":["Containers in a pod share the network and can share volumes.","Sidecar pattern: helper container alongside the main app.","Init containers run to completion before app containers start.","Pods are mortal — replaced, not healed, on failure.","Each pod gets a cluster-internal IP."],
     "code":[("yaml","apiVersion: v1\nkind: Pod\nmetadata:\n  name: web\nspec:\n  containers:\n    - name: app\n      image: nginx:1.27\n      ports: [{containerPort: 80}]\n      resources:\n        requests: {cpu: \"100m\", memory: \"128Mi\"}\n        limits: {cpu: \"500m\", memory: \"256Mi\"}")],
     "example":{"title":"Init container","text":"Wait for a dependency before starting the app:","code":[("yaml","initContainers:\n  - name: wait-db\n    image: busybox\n    command: ['sh','-c','until nc -z db 5432; do sleep 1; done']")]},
     "best":["Manage pods via Deployments, not directly.","Set resource requests and limits.","Use sidecars for logging/proxy concerns.","Use init containers for setup/wait logic.","Add liveness/readiness probes."],
     "pitfalls":["Creating bare pods (no self-healing/rollout).","Missing resource requests → poor scheduling/eviction.","Assuming a pod IP is stable (it isn't across restarts).","Putting unrelated apps in one pod.","No probes → traffic to unready pods.","Relying on pod-local storage for persistence."],
     "interview":[("What is a pod?","The smallest deployable unit: one or more containers sharing network/storage namespaces."),("Why multiple containers in a pod?","Tightly-coupled helpers (sidecars) that must share IP/volumes, e.g., a log shipper or proxy."),("Init containers vs sidecars?","Init containers run to completion before app start; sidecars run alongside the app."),("Are pods self-healing?","Not by themselves — controllers like Deployments recreate failed pods."),("Why set requests/limits?","Requests guide scheduling; limits cap usage and prevent noisy neighbors."),("Is a pod IP stable?","No — it changes on recreation; use Services for stable access."),("What are probes?","Liveness (restart if dead) and readiness (gate traffic) health checks."),("Should you create pods directly?","Rarely — use Deployments/Jobs for lifecycle management.")],
     "practice":["Write a pod with requests/limits and a probe.","Add an init container that waits for a service.","Explain why a bare pod won't self-heal."],
     "revision":"Pod = smallest unit, 1+ containers sharing network/storage, ephemeral. Use Deployments to manage them; set requests/limits and probes; sidecars/init containers for helpers; pod IPs aren't stable."},

    {"id":3,"slug":"03-deployments","name":"Deployments & ReplicaSets","category":"Core Objects","level":"Beginner",
     "summary":"Declarative, self-healing, rolling-updated pod management.",
     "keywords":["deployment","replicaset","rolling update","rollback","replicas"],"refs":["Deployments"],
     "overview":"A **Deployment** manages a **ReplicaSet** that keeps a desired number of identical pods running. It provides self-healing, **rolling updates** (gradual replacement), and **rollbacks** — the standard way to run stateless apps.",
     "concepts":["Deployment → ReplicaSet → Pods (ownership chain).","Rolling update replaces pods gradually (maxSurge/maxUnavailable).","Rollback to a previous revision on failure.","Self-healing: failed pods are recreated to match replicas.","Selectors/labels tie a Deployment to its pods."],
     "code":[("yaml","apiVersion: apps/v1\nkind: Deployment\nmetadata: {name: web}\nspec:\n  replicas: 3\n  selector: {matchLabels: {app: web}}\n  strategy:\n    rollingUpdate: {maxSurge: 1, maxUnavailable: 0}\n  template:\n    metadata: {labels: {app: web}}\n    spec:\n      containers:\n        - {name: web, image: myapp:1.2}")],
     "example":{"title":"Update and rollback","text":"Ship a new image, then revert if it misbehaves:","code":[("bash","kubectl set image deploy/web web=myapp:1.3\nkubectl rollout status deploy/web\nkubectl rollout undo deploy/web   # rollback")]},
     "best":["Use Deployments for stateless apps.","Set maxUnavailable=0 for zero-downtime rollouts.","Add readiness probes so rollouts wait for healthy pods.","Keep replicas ≥ 2 for availability.","Version images (no latest) for reliable rollbacks."],
     "pitfalls":["latest image tags breaking rollback/repeatability.","No readiness probe → traffic to unready pods mid-rollout.","maxUnavailable too high causing downtime.","Mismatched selector/labels orphaning pods.","Single replica = no availability.","Manual pod edits fighting the controller."],
     "interview":[("Deployment vs ReplicaSet?","A Deployment manages ReplicaSets and adds rolling updates/rollbacks; ReplicaSet just maintains replica count."),("How do rolling updates work?","Pods are replaced gradually per maxSurge/maxUnavailable, keeping the app available."),("How to roll back?","kubectl rollout undo to a prior revision."),("How is self-healing achieved?","The controller recreates pods to match the declared replica count."),("Why readiness probes during rollout?","So new pods receive traffic only when healthy, preventing errors."),("Zero-downtime config?","maxUnavailable=0, maxSurge>=1, plus readiness probes and ≥2 replicas."),("Why avoid latest tags?","Rollbacks/repeatability break when the tag's content changes."),("How does a Deployment select its pods?","Via label selectors matching the pod template's labels.")],
     "practice":["Create a 3-replica Deployment and update its image.","Trigger and watch a rolling update with rollout status.","Roll back to the previous revision."],
     "revision":"Deployment → ReplicaSet → Pods: self-healing + rolling updates + rollback. Versioned images, readiness probes, maxUnavailable=0, ≥2 replicas for zero-downtime."},

    {"id":4,"slug":"04-services","name":"Services","category":"Networking","level":"Intermediate",
     "summary":"Stable virtual endpoints load-balancing to pods.",
     "keywords":["service","clusterip","nodeport","loadbalancer","selector"],"refs":["Services"],
     "overview":"Pods are ephemeral with changing IPs; a **Service** gives a stable virtual IP/DNS name that load-balances to a set of pods selected by labels. Types: **ClusterIP** (internal), **NodePort** (host port), **LoadBalancer** (cloud LB), plus **Headless** for direct pod discovery.",
     "concepts":["ClusterIP: stable internal VIP (default).","NodePort: exposes a port on every node.","LoadBalancer: provisions an external cloud load balancer.","Selector matches pod labels to build the endpoint set.","DNS: service reachable as name.namespace.svc.cluster.local."],
     "code":[("yaml","apiVersion: v1\nkind: Service\nmetadata: {name: web}\nspec:\n  selector: {app: web}\n  ports:\n    - {port: 80, targetPort: 8080}\n  type: ClusterIP")],
     "example":{"title":"Service discovery by DNS","text":"Other pods reach it by name:","code":[("bash","curl http://web.default.svc.cluster.local/\n# or just http://web within the same namespace")]},
     "best":["Use ClusterIP for internal traffic; Ingress/LB for external.","Rely on Service DNS, not pod IPs.","Keep selectors aligned with pod labels.","Use readiness probes so only ready pods get traffic.","Prefer Ingress over many LoadBalancers for HTTP."],
     "pitfalls":["Selector/label mismatch → empty endpoints (no traffic).","Using NodePort for production external access.","Confusing port (service) with targetPort (container).","Assuming a Service heals pods (it only routes).","One LoadBalancer per service getting expensive.","Forgetting cross-namespace DNS suffix."],
     "interview":[("Why do we need Services?","Pods have ephemeral IPs; a Service provides a stable VIP/DNS and load-balances across matching pods."),("ClusterIP vs NodePort vs LoadBalancer?","Internal VIP / a port on each node / an external cloud LB, respectively."),("How does a Service find its pods?","Via a label selector that builds the endpoints list."),("port vs targetPort?","port is the Service's port; targetPort is the container port it forwards to."),("What is a headless service?","clusterIP: None — returns pod IPs directly (for stateful/peer discovery)."),("How does Service DNS work?","name.namespace.svc.cluster.local resolves to the ClusterIP."),("Service vs Ingress?","Service does L4 routing; Ingress does L7 HTTP routing/host-path rules."),("Why might endpoints be empty?","No ready pods match the selector, or labels mismatch.")],
     "practice":["Expose a Deployment with a ClusterIP service.","Reach it via Service DNS from another pod.","Diagnose an empty-endpoints selector mismatch."],
     "revision":"Service = stable VIP/DNS load-balancing to label-selected pods. ClusterIP (internal), NodePort (node port), LoadBalancer (cloud), Headless (direct). Use DNS, align labels, Ingress for HTTP."},

    {"id":5,"slug":"05-ingress","name":"Ingress & Gateway","category":"Networking","level":"Intermediate",
     "summary":"L7 HTTP routing by host/path with TLS termination.",
     "keywords":["ingress","controller","tls","host","path routing"],"refs":["Ingress"],
     "overview":"An **Ingress** exposes HTTP(S) routes from outside the cluster to Services, based on **host** and **path**, with **TLS** termination — all behind one external IP. It requires an **Ingress controller** (nginx, Traefik, cloud) to do the actual routing.",
     "concepts":["Ingress = rules; an Ingress controller enforces them.","Route by host (api.example.com) and path (/v1).","TLS termination via referenced secrets.","One entry point for many services (cheaper than many LBs).","The Gateway API is the newer, more expressive successor."],
     "code":[("yaml","apiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata: {name: web}\nspec:\n  tls: [{hosts: [app.example.com], secretName: app-tls}]\n  rules:\n    - host: app.example.com\n      http:\n        paths:\n          - {path: /, pathType: Prefix, backend: {service: {name: web, port: {number: 80}}}}")],
     "example":{"title":"Path-based routing","text":"Send /api to one service, / to another:","code":[("yaml","paths:\n  - {path: /api, pathType: Prefix, backend: {service: {name: api, port: {number: 80}}}}\n  - {path: /,    pathType: Prefix, backend: {service: {name: web, port: {number: 80}}}}")]},
     "best":["Install an Ingress controller before creating Ingress objects.","Consolidate services behind one Ingress to save LBs.","Terminate TLS at Ingress with managed certs (cert-manager).","Use clear host/path rules; mind pathType.","Consider the Gateway API for advanced routing."],
     "pitfalls":["Creating Ingress with no controller installed (nothing happens).","pathType confusion (Prefix vs Exact).","Missing/expired TLS secrets.","Annotations differing per controller (portability).","Routing conflicts/overlaps between rules.","Assuming Ingress does L4/TCP (it's HTTP-focused)."],
     "interview":[("What is an Ingress?","An L7 routing object mapping host/path HTTP traffic to Services, with TLS, behind one entry point."),("Ingress vs Service LoadBalancer?","Ingress does HTTP host/path routing for many services through one LB; per-service LBs are L4 and costlier."),("Why is an Ingress controller required?","The Ingress object is just rules; a controller (nginx/Traefik/cloud) implements them."),("How is TLS handled?","Terminated at the Ingress using a referenced TLS secret (often via cert-manager)."),("pathType options?","Prefix, Exact, ImplementationSpecific — control how paths match."),("What is the Gateway API?","A newer, role-oriented, more expressive successor to Ingress."),("Can Ingress route TCP/UDP?","Not natively; it's HTTP(S) — use Service/LB or controller-specific extensions."),("Why consolidate behind Ingress?","Fewer external load balancers and centralized TLS/routing.")],
     "practice":["Deploy an nginx Ingress controller and an Ingress.","Add host + path routing for two services.","Terminate TLS with a secret/cert-manager."],
     "revision":"Ingress = L7 HTTP routing (host/path) + TLS behind one entry, enforced by a controller. Consolidates services, terminates TLS; mind pathType and controller annotations; Gateway API is its successor."},

    {"id":6,"slug":"06-config-secrets","name":"ConfigMaps & Secrets","category":"Config & State","level":"Intermediate",
     "summary":"Externalize configuration and sensitive data from images.",
     "keywords":["configmap","secret","env","volume","base64"],"refs":["ConfigMaps; Secrets"],
     "overview":"**ConfigMaps** hold non-sensitive config; **Secrets** hold sensitive data (base64-encoded, ideally encrypted at rest). Both inject into pods as **environment variables** or mounted **files**, keeping config out of images for the 12-factor app pattern.",
     "concepts":["ConfigMap = key/value config; Secret = sensitive key/value.","Inject as env vars or mounted volume files.","Secrets are base64 (not encrypted by default) — enable encryption at rest.","Mounted config can update without rebuilding images.","Use external secret managers for stronger security."],
     "code":[("yaml","apiVersion: v1\nkind: ConfigMap\nmetadata: {name: app-config}\ndata:\n  LOG_LEVEL: info\n---\napiVersion: v1\nkind: Secret\nmetadata: {name: app-secret}\ntype: Opaque\nstringData:\n  DB_PASSWORD: s3cr3t")],
     "example":{"title":"Consume in a pod","text":"Inject config as env and secret as a file:","code":[("yaml","envFrom: [{configMapRef: {name: app-config}}]\nvolumeMounts: [{name: sec, mountPath: /etc/secret, readOnly: true}]\nvolumes: [{name: sec, secret: {secretName: app-secret}}]")]},
     "best":["Keep all config out of images (12-factor).","Use Secrets for credentials; enable etcd encryption at rest.","Prefer external secret managers (Vault, cloud KMS) for prod.","Restrict access via RBAC.","Mount config as files when hot-reload is desired."],
     "pitfalls":["Treating base64 as encryption (it isn't).","Committing Secret manifests to git in plaintext.","Baking config into images.","Broad RBAC exposing all Secrets.","Env-var secrets leaking via crash dumps/logs.","Forgetting pods need restart for env (not file) updates."],
     "interview":[("ConfigMap vs Secret?","Both store key/values for injection; Secrets are for sensitive data (base64, RBAC-restricted, encryptable at rest)."),("Are Secrets encrypted?","Base64-encoded by default; enable encryption at rest and/or use an external secret manager."),("How are they consumed?","As environment variables or mounted as files in the pod."),("Why externalize config?","12-factor: same image across environments, config injected at runtime."),("Do env updates hot-reload?","No — changing a ConfigMap doesn't update env in a running pod; mounted files can update."),("How to secure Secrets?","RBAC, encryption at rest, external managers, and avoid logging them."),("Risk of committing Secrets to git?","Plaintext/base64 exposure; use sealed-secrets or external stores."),("envFrom vs valueFrom?","envFrom injects all keys; valueFrom maps a single key to a variable.")],
     "practice":["Inject a ConfigMap as env and a Secret as a file.","Enable a hot-reloadable mounted config.","Restrict Secret access via RBAC."],
     "revision":"ConfigMaps (config) + Secrets (sensitive, base64/RBAC/encrypt-at-rest) inject as env or files. Keep config out of images; use external secret managers in prod; env changes need restart, files can hot-reload."},

    {"id":7,"slug":"07-volumes-statefulsets","name":"Persistent Volumes & StatefulSets","category":"Config & State","level":"Advanced",
     "summary":"Durable storage and stable identity for stateful apps.",
     "keywords":["pv","pvc","storageclass","statefulset","persistent"],"refs":["Persistent Volumes; StatefulSets"],
     "overview":"For stateful workloads, **PersistentVolumes (PV)** provide durable storage, claimed by pods via **PersistentVolumeClaims (PVC)**, often provisioned dynamically by a **StorageClass**. **StatefulSets** give pods stable network identities and ordered, persistent storage (databases, queues).",
     "concepts":["PVC requests storage; PV (often dynamic via StorageClass) satisfies it.","StatefulSet pods have stable names (web-0, web-1) and per-pod PVCs.","Ordered, graceful deployment/scaling/deletion.","Headless service gives stable per-pod DNS.","Access modes: RWO, ROX, RWX (storage-dependent)."],
     "code":[("yaml","apiVersion: apps/v1\nkind: StatefulSet\nmetadata: {name: db}\nspec:\n  serviceName: db\n  replicas: 3\n  selector: {matchLabels: {app: db}}\n  template:\n    metadata: {labels: {app: db}}\n    spec:\n      containers: [{name: db, image: postgres:16, volumeMounts: [{name: data, mountPath: /var/lib/postgresql/data}]}]\n  volumeClaimTemplates:\n    - metadata: {name: data}\n      spec: {accessModes: [ReadWriteOnce], resources: {requests: {storage: 10Gi}}}")],
     "example":{"title":"Stable identity","text":"Each replica keeps its name and its own volume across restarts:","code":[("bash","kubectl get pods -l app=db   # db-0, db-1, db-2 with persistent PVCs")]},
     "best":["Use StatefulSets for databases/queues needing identity+storage.","Use dynamic provisioning via StorageClass.","Match access modes to the storage backend.","Back up PVs/volumes regularly.","Use a headless service for stable pod DNS."],
     "pitfalls":["Using a Deployment for stateful apps (no stable identity/storage).","Assuming RWX works on block storage (often RWO only).","Deleting a StatefulSet without handling PVC retention.","Ignoring ordered scaling implications.","No backups of persistent data.","Reclaim policy deleting data unexpectedly."],
     "interview":[("PV vs PVC?","PV is the actual storage resource; PVC is a pod's request that binds to a PV."),("What does a StorageClass do?","Defines a provisioner/parameters for dynamic PV creation on demand."),("Deployment vs StatefulSet?","StatefulSet gives stable identities (ordinal names), ordered ops, and per-pod persistent volumes."),("What are access modes?","RWO (one node RW), ROX (many read-only), RWX (many RW) — backend-dependent."),("Why a headless service with StatefulSets?","To give each pod a stable DNS name for peer discovery."),("What is a reclaim policy?","What happens to a PV when its PVC is deleted: Retain, Delete, or Recycle."),("volumeClaimTemplates purpose?","Auto-creates a unique PVC per StatefulSet pod."),("How to scale a database StatefulSet safely?","Ordered scaling plus app-level replication/clustering and backups.")],
     "practice":["Create a StatefulSet with volumeClaimTemplates.","Verify stable pod names and per-pod PVCs.","Set a Retain reclaim policy and explain why."],
     "revision":"Stateful apps: PVC→PV (dynamic via StorageClass) for durable storage; StatefulSets add stable identity + per-pod volumes + ordered ops + headless DNS. Mind access modes, reclaim policy, and backups."},

    {"id":8,"slug":"08-autoscaling","name":"Autoscaling (HPA/VPA/Cluster)","category":"Scaling & Scheduling","level":"Advanced",
     "summary":"Scale pods and nodes automatically to match load.",
     "keywords":["hpa","vpa","cluster autoscaler","metrics","scaling"],"refs":["HPA"],
     "overview":"Kubernetes scales at three levels: **HPA** adds/removes pod replicas based on metrics (CPU/memory/custom), **VPA** adjusts pod resource requests, and the **Cluster Autoscaler** adds/removes nodes when pods can't be scheduled. Requests/limits and the metrics-server underpin all of it.",
     "concepts":["HPA scales replicas to hit a target metric (e.g., 70% CPU).","Needs metrics-server (or custom/external metrics adapter).","VPA right-sizes requests/limits (often recommend-only).","Cluster Autoscaler grows/shrinks the node pool.","Accurate requests are essential for correct scaling."],
     "code":[("bash","kubectl autoscale deploy web --cpu-percent=70 --min=2 --max=10\nkubectl get hpa web")],
     "example":{"title":"HPA manifest","text":"Declarative target utilization:","code":[("yaml","apiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nspec:\n  scaleTargetRef: {kind: Deployment, name: web}\n  minReplicas: 2\n  maxReplicas: 10\n  metrics: [{type: Resource, resource: {name: cpu, target: {type: Utilization, averageUtilization: 70}}}]")]},
     "best":["Set accurate resource requests (HPA bases CPU% on requests).","Install metrics-server for resource metrics.","Use custom/external metrics for queue-driven scaling.","Combine HPA + Cluster Autoscaler for full elasticity.","Tune min/max and stabilization to avoid flapping."],
     "pitfalls":["HPA without metrics-server (no scaling).","Missing/incorrect requests skewing CPU%.","Running HPA and VPA on the same resource conflicting.","Scaling thrash from tight thresholds (add stabilization).","maxReplicas too low to absorb spikes.","Forgetting nodes also need to scale (Cluster Autoscaler)."],
     "interview":[("What does HPA scale?","The number of pod replicas, based on observed metrics vs a target."),("HPA prerequisite?","A metrics source (metrics-server) and meaningful resource requests."),("HPA vs VPA?","HPA changes replica count; VPA changes per-pod resource requests/limits."),("What is the Cluster Autoscaler?","It adds/removes nodes when pods are unschedulable or nodes are underutilized."),("Why are requests critical for HPA?","CPU utilization is computed as usage/requests; wrong requests mislead scaling."),("How to scale on a queue length?","Custom/external metrics (e.g., KEDA) feeding the HPA."),("How to prevent flapping?","Stabilization windows and sensible thresholds/min-max."),("Can HPA and VPA coexist?","Not on the same resource metric without conflict; combine carefully or use VPA in recommend mode.")],
     "practice":["Create an HPA targeting 70% CPU with min/max.","Load-test and watch replicas scale.","Explain how Cluster Autoscaler complements HPA."],
     "revision":"HPA scales replicas on metrics (needs metrics-server + accurate requests), VPA right-sizes requests, Cluster Autoscaler scales nodes. Combine HPA+CA; tune min/max + stabilization to avoid thrash."},

    {"id":9,"slug":"09-scheduling","name":"Scheduling, Affinity & Taints","category":"Scaling & Scheduling","level":"Advanced",
     "summary":"Control which nodes pods land on.",
     "keywords":["scheduling","affinity","taint","toleration","node selector"],"refs":["Assigning pods to nodes"],
     "overview":"The scheduler places pods on nodes using resource requests plus constraints: **nodeSelector/affinity** (attract pods to nodes), **taints/tolerations** (repel pods unless tolerated), and **pod (anti-)affinity** (co-locate or spread pods). These ensure performance, isolation, and high availability.",
     "concepts":["nodeSelector/nodeAffinity steer pods to labeled nodes.","Taints repel pods; matching tolerations allow scheduling.","Pod anti-affinity spreads replicas across nodes/zones.","topologySpreadConstraints balance across topology domains.","Requests must fit a node's allocatable resources."],
     "code":[("yaml","affinity:\n  podAntiAffinity:\n    requiredDuringSchedulingIgnoredDuringExecution:\n      - labelSelector: {matchLabels: {app: web}}\n        topologyKey: kubernetes.io/hostname  # spread across nodes\ntolerations:\n  - {key: gpu, operator: Exists, effect: NoSchedule}")],
     "example":{"title":"Dedicate nodes via taints","text":"Taint GPU nodes; only GPU pods tolerate them:","code":[("bash","kubectl taint nodes gpu-1 gpu=true:NoSchedule")]},
     "best":["Spread replicas with anti-affinity/topology constraints for HA.","Use taints to reserve nodes (GPU/spot/system).","Label nodes meaningfully for affinity.","Keep requests realistic so scheduling succeeds.","Prefer soft (preferred) rules unless hard placement is required."],
     "pitfalls":["Over-constraining → pods Pending (unschedulable).","Taints without tolerations stranding workloads.","All replicas on one node (no anti-affinity) → single point of failure.","Confusing node affinity (pods↔nodes) with pod affinity (pods↔pods).","Hard rules where soft would do.","Ignoring resource fit when adding constraints."],
     "interview":[("How does the scheduler choose a node?","Filters nodes by requests/constraints, then scores and picks the best fit."),("nodeSelector vs nodeAffinity?","Both attract pods to nodes; affinity is more expressive (operators, soft/hard)."),("Taints and tolerations?","Taints repel pods from nodes; only pods with matching tolerations schedule there."),("Pod anti-affinity use?","Spread replicas across nodes/zones for high availability."),("Why might a pod stay Pending?","No node satisfies its requests/constraints (over-constrained or insufficient resources)."),("topologySpreadConstraints?","Evenly distribute pods across topology domains (zones/nodes)."),("Hard vs soft rules?","required (must) vs preferred (best-effort) scheduling constraints."),("Reserve nodes for special workloads?","Taint the nodes and add matching tolerations to those workloads.")],
     "practice":["Spread a Deployment with pod anti-affinity.","Taint a node and schedule a tolerating pod.","Diagnose a Pending pod from over-constrained affinity."],
     "revision":"Scheduler = requests + constraints. Affinity attracts (pods↔nodes), taints/tolerations repel, anti-affinity/topology spread for HA. Avoid over-constraining (Pending); reserve nodes with taints."},

    {"id":10,"slug":"10-troubleshooting","name":"Observability & Troubleshooting","category":"Operations","level":"Advanced",
     "summary":"Diagnose failing pods with kubectl and probes.",
     "keywords":["kubectl","logs","describe","events","crashloop","probe"],"refs":["Debug pods"],
     "overview":"Troubleshooting Kubernetes is methodical: `kubectl describe` (events), `kubectl logs` (app output), `kubectl get events`, and probes reveal why pods fail. Common states — **CrashLoopBackOff**, **ImagePullBackOff**, **Pending** — each point to a specific class of problem.",
     "concepts":["describe shows events, conditions, and probe results.","logs (and --previous) show app output and crash reasons.","CrashLoopBackOff = container keeps exiting; check logs/command.","ImagePullBackOff = bad image/registry/credentials.","Pending = unschedulable (resources/constraints/PVC unbound)."],
     "code":[("bash","kubectl describe pod web-abc\nkubectl logs web-abc --previous   # last crashed container\nkubectl get events --sort-by=.lastTimestamp\nkubectl exec -it web-abc -- sh")],
     "example":{"title":"Triaging CrashLoopBackOff","text":"Read logs of the crashed instance and its exit code:","code":[("bash","kubectl logs web-abc --previous\nkubectl describe pod web-abc | grep -A5 'Last State'")]},
     "best":["Start with describe (events) then logs.","Add liveness/readiness probes for clear health signals.","Centralize logs/metrics (Prometheus/Grafana/Loki).","Use --previous to inspect crashed containers.","Check resource limits when pods are OOMKilled."],
     "pitfalls":["Restarting pods blindly without reading events/logs.","Liveness probe too aggressive causing restart loops.","Ignoring OOMKilled (raise limits or fix leak).","Pending pods due to unbound PVCs overlooked.","No observability stack in production.","Confusing readiness (traffic gating) with liveness (restart)."],
     "interview":[("First steps to debug a failing pod?","kubectl describe (events/conditions) then kubectl logs (and --previous)."),("What causes CrashLoopBackOff?","The container repeatedly exits — bad command, missing config, or a crashing app; check logs."),("ImagePullBackOff cause?","Wrong image name/tag, private registry without credentials, or registry unreachable."),("Why is a pod Pending?","No node can schedule it: insufficient resources, constraints, or an unbound PVC."),("Liveness vs readiness probe?","Liveness restarts a dead container; readiness gates traffic until ready."),("What is OOMKilled?","The container exceeded its memory limit and was killed; raise limits or fix the leak."),("How to inspect a crashed container's logs?","kubectl logs <pod> --previous."),("Why centralize observability?","Ephemeral pods lose local logs; aggregation enables search, dashboards, and alerts.")],
     "practice":["Diagnose a CrashLoopBackOff via describe + logs --previous.","Reproduce ImagePullBackOff and fix the image ref.","Resolve a Pending pod caused by resource requests."],
     "revision":"Debug methodically: describe (events) → logs (--previous) → events/exec. CrashLoopBackOff=app exits, ImagePullBackOff=image/creds, Pending=unschedulable, OOMKilled=memory. Probes + centralized observability."},
  ],
}
