# Docker Handbook spec
SPEC = {
  "id": "docker", "name": "Docker Handbook", "icon": "🐳",
  "tagline": "Master containers for interviews and production: images, Dockerfiles, networking, volumes, Compose, and security.",
  "itemNoun": "Topic",
  "levels": ["Beginner", "Intermediate", "Advanced"],
  "categories": [
    {"name": "Fundamentals", "level": "Beginner", "icon": "📦"},
    {"name": "Images & Builds", "level": "Beginner", "icon": "🏗️"},
    {"name": "Runtime", "level": "Intermediate", "icon": "🔀"},
    {"name": "Compose & Orchestration", "level": "Intermediate", "icon": "🎼"},
    {"name": "Production", "level": "Advanced", "icon": "🛡️"},
  ],
  "items": [
    {"id":1,"slug":"01-containers-vs-vms","name":"Containers vs VMs","category":"Fundamentals","level":"Beginner",
     "summary":"Containers share the host kernel; VMs virtualize hardware.",
     "keywords":["container","vm","kernel","namespace","cgroup","isolation"],"refs":["Docker overview"],
     "overview":"A **container** packages an app with its dependencies and runs as an isolated process on the host's kernel, using Linux **namespaces** (isolation) and **cgroups** (resource limits). Unlike a **VM**, it doesn't boot a full guest OS, so it's lighter and starts in milliseconds.",
     "concepts":["Containers share the host kernel; VMs each run a guest OS.","Namespaces isolate PID, network, mount, user, etc.","cgroups limit CPU/memory/IO per container.","Images are read-only layers; the container adds a writable layer.","Faster startup and higher density than VMs."],
     "code":[("bash","docker run --rm -it alpine:3.20 sh   # start a tiny container shell\n# inside: ps shows an isolated process tree")],
     "example":{"title":"Resource limits via cgroups","text":"Cap memory and CPU:","code":[("bash","docker run --memory=256m --cpus=0.5 nginx")]},
     "best":["Use containers for consistent, portable environments.","Set resource limits (--memory/--cpus) in production.","Keep one main process per container.","Prefer small base images (alpine/distroless).","Treat containers as ephemeral and immutable."],
     "pitfalls":["Treating containers like VMs (multiple services, SSH inside).","Assuming containers provide VM-level security isolation.","Running without resource limits (noisy neighbor).","Storing important data in the writable layer (lost on removal).","Mismatched host/container architecture.","Expecting a different kernel than the host's."],
     "interview":[("Container vs VM?","Containers share the host kernel and isolate via namespaces/cgroups (lightweight); VMs virtualize hardware and run a full guest OS."),("What provides container isolation?","Linux namespaces (PID/net/mount/user) for isolation and cgroups for resource limits."),("Why do containers start fast?","No OS boot — they're just isolated host processes."),("Are containers as secure as VMs?","Generally weaker isolation (shared kernel); use extra hardening or VMs for strict multi-tenancy."),("What is an image layer?","A read-only filesystem diff; images stack layers, containers add a writable top layer."),("One process per container — why?","Simpler lifecycle, logging, scaling, and signal handling."),("How to limit resources?","--cpus/--memory map to cgroup limits."),("Where should persistent data go?","In volumes, not the container's writable layer.")],
     "practice":["Run an Alpine container and inspect its isolated process tree.","Apply memory and CPU limits to a container.","Explain why a container can't run a different-kernel OS."],
     "revision":"Containers = isolated host processes (namespaces + cgroups), sharing the kernel — lighter/faster than VMs. One process each, set limits, keep ephemeral, persist data in volumes."},

    {"id":2,"slug":"02-images-layers","name":"Images & Layers","category":"Images & Builds","level":"Beginner",
     "summary":"Immutable, layered, content-addressed filesystem bundles.",
     "keywords":["image","layer","tag","registry","cache"],"refs":["About images"],
     "overview":"A Docker **image** is a read-only template built from stacked **layers**, each created by a Dockerfile instruction. Layers are cached and shared across images, making builds and pulls fast. Images are tagged and stored in **registries** (Docker Hub, ECR, GHCR).",
     "concepts":["Each Dockerfile instruction creates a cacheable layer.","Layers are shared/deduplicated across images.","Tags (name:tag) are mutable pointers; digests are immutable.","Registries store and distribute images.","Build cache invalidates from the first changed layer downward."],
     "code":[("bash","docker pull python:3.12-slim\ndocker images          # list local images\ndocker history python:3.12-slim   # see layers\ndocker tag myapp:dev registry.example.com/myapp:1.0")],
     "example":{"title":"Layer cache ordering","text":"Copy dependency manifests before source so deps cache across code edits:","code":[("dockerfile","COPY requirements.txt .\nRUN pip install -r requirements.txt   # cached unless requirements change\nCOPY . .                               # changes often, kept last")]},
     "best":["Order Dockerfile steps from least- to most-frequently changing.","Pin base image versions/digests for reproducibility.","Use .dockerignore to shrink build context.","Tag images with semantic versions, not just latest.","Reference immutable digests in production."],
     "pitfalls":["Copying source before installing deps (busts cache every build).","Relying on the mutable latest tag in prod.","Huge build context (no .dockerignore).","Secrets baked into layers (they persist in history).","Assuming deleting a file in a later layer reduces size (it doesn't).","Not cleaning package caches in the same RUN."],
     "interview":[("What is a Docker image?","An immutable, layered, content-addressed filesystem + metadata used to create containers."),("How does build caching work?","Each instruction is a layer; cache is reused until an instruction or its inputs change, then all later layers rebuild."),("Tag vs digest?","Tags are mutable human labels; digests (sha256) are immutable content identifiers."),("Why order Dockerfile steps carefully?","To maximize cache hits — put stable steps (dep install) before volatile ones (source copy)."),("Why are secrets in layers dangerous?","Layers are kept in image history; anyone with the image can extract them."),("What's a registry?","A service that stores and distributes images (Docker Hub, ECR, GHCR)."),("Does deleting a file in a later layer shrink the image?","No — the earlier layer still contains it; remove within the same RUN."),("What is .dockerignore for?","Excluding files from the build context to speed builds and avoid leaking files.")],
     "practice":["Inspect an image's layers with docker history.","Reorder a Dockerfile to improve cache hits.","Add a .dockerignore and measure context size."],
     "revision":"Images = immutable stacked layers (cached, shared), tagged in registries. Order Dockerfile stable→volatile, pin versions/digests, use .dockerignore, never bake secrets into layers."},

    {"id":3,"slug":"03-dockerfile","name":"Writing Dockerfiles","category":"Images & Builds","level":"Intermediate",
     "summary":"Declarative build recipes — instruction by instruction.",
     "keywords":["dockerfile","from","run","copy","cmd","entrypoint"],"refs":["Dockerfile reference"],
     "overview":"A **Dockerfile** is a script of instructions that builds an image: `FROM` (base), `RUN` (execute), `COPY`/`ADD` (files), `ENV`/`ARG` (config), `EXPOSE`, and `CMD`/`ENTRYPOINT` (default process). Good Dockerfiles are small, cache-friendly, and reproducible.",
     "concepts":["FROM sets the base; one per build stage.","RUN executes at build time and creates a layer.","CMD provides default args; ENTRYPOINT sets the executable.","ENV/ARG configure build/runtime; ARG is build-only.","WORKDIR sets the working directory for later instructions."],
     "code":[("dockerfile","FROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nEXPOSE 8000\nCMD [\"gunicorn\", \"app:app\", \"--bind\", \"0.0.0.0:8000\"]")],
     "example":{"title":"ENTRYPOINT + CMD","text":"ENTRYPOINT fixes the binary; CMD gives overridable default args:","code":[("dockerfile","ENTRYPOINT [\"python\", \"manage.py\"]\nCMD [\"runserver\", \"0.0.0.0:8000\"]\n# docker run img migrate  -> overrides CMD, keeps entrypoint")]},
     "best":["Use exec form (JSON array) for CMD/ENTRYPOINT to handle signals.","Combine related RUN steps and clean caches in the same layer.","Prefer COPY over ADD unless you need URL/tar extraction.","Set a non-root USER.","Use --no-cache-dir / package cleanup to keep images small."],
     "pitfalls":["Shell form CMD swallowing signals (no graceful shutdown).","Many RUN layers bloating the image.","ADD used where COPY is safer/clearer.","Running as root by default.","apt-get update in a separate layer from install (stale cache).","Not pinning package versions."],
     "interview":[("CMD vs ENTRYPOINT?","ENTRYPOINT sets the executable; CMD provides default arguments. Together: fixed binary + overridable args."),("COPY vs ADD?","COPY just copies; ADD also fetches URLs and auto-extracts tars — prefer COPY unless you need those."),("Why exec form over shell form?","Exec form runs the process as PID 1 so it receives signals (SIGTERM) for graceful shutdown."),("ARG vs ENV?","ARG is available only at build time; ENV persists into the running container."),("How to keep images small?","Slim/distroless base, combined RUN with cleanup, multi-stage builds, .dockerignore."),("Why a non-root USER?","Limits blast radius if the container is compromised."),("Why combine apt update+install?","Separate layers can cache a stale package index; combine to stay consistent."),("What does WORKDIR do?","Sets the cwd for subsequent RUN/CMD/COPY and creates it if missing.")],
     "practice":["Write a Dockerfile for a Python web app.","Add ENTRYPOINT+CMD and override CMD at runtime.","Switch to a non-root USER and verify."],
     "revision":"Dockerfile builds images: FROM/RUN/COPY/ENV/CMD/ENTRYPOINT. Use exec form, combine+clean RUN steps, COPY over ADD, run as non-root, pin versions, keep cache-friendly."},

    {"id":4,"slug":"04-multistage-builds","name":"Multi-Stage Builds","category":"Images & Builds","level":"Advanced",
     "summary":"Build in one stage, ship only the artifacts in a tiny final image.",
     "keywords":["multi-stage","builder","artifact","slim","distroless"],"refs":["Multi-stage builds"],
     "overview":"**Multi-stage builds** use multiple `FROM` stages: a heavy *builder* compiles/installs, then a minimal *final* stage copies just the runtime artifacts. This yields tiny, secure images without build tools, source, or caches.",
     "concepts":["Each FROM starts a new stage; name them with AS.","COPY --from=<stage> pulls only needed artifacts.","Final image excludes compilers/build deps.","Great for compiled langs (Go) and trimmed runtimes.","Combine with distroless/scratch for minimal surface."],
     "code":[("dockerfile","# stage 1: build\nFROM golang:1.22 AS builder\nWORKDIR /src\nCOPY . .\nRUN CGO_ENABLED=0 go build -o /app ./cmd/server\n\n# stage 2: minimal runtime\nFROM gcr.io/distroless/static\nCOPY --from=builder /app /app\nENTRYPOINT [\"/app\"]")],
     "example":{"title":"Node build then serve","text":"Compile assets in a builder, serve with a slim image:","code":[("dockerfile","FROM node:20 AS build\nWORKDIR /app\nCOPY . . \nRUN npm ci && npm run build\nFROM nginx:alpine\nCOPY --from=build /app/dist /usr/share/nginx/html")]},
     "best":["Put build tools only in the builder stage.","Copy just the final binary/assets into the runtime stage.","Use scratch/distroless for compiled binaries.","Name stages for clarity (AS builder).","Cache dependency install before copying full source."],
     "pitfalls":["Copying the whole builder filesystem into final (defeats purpose).","Dynamic binaries needing libs absent in scratch/distroless.","Forgetting CGO_ENABLED=0 for static Go binaries.","Re-installing deps in the final stage.","Not leveraging build cache across stages.","Leaking secrets used in the builder into final via COPY."],
     "interview":[("What problem do multi-stage builds solve?","They produce small, secure final images by excluding build tools, source, and caches."),("How do you pull artifacts between stages?","COPY --from=<stage-name-or-index>."),("Why distroless/scratch?","Minimal base with no shell/package manager → smaller and far less attack surface."),("Go static binary requirement?","CGO_ENABLED=0 (and static linking) so it runs without libc in scratch."),("Do earlier stages ship in the final image?","No — only what you COPY --from is included."),("How to keep deps cached?","Copy lockfiles and install before copying the full source, like single-stage builds."),("Trade-off of distroless?","No shell for debugging; use a debug variant or temporary tooling."),("Multi-stage for interpreted langs?","Yes — build/compile assets in one stage, copy into a slim runtime.")],
     "practice":["Convert a single-stage Go build to multi-stage + distroless.","Build a frontend in one stage and serve via nginx.","Compare image sizes before/after."],
     "revision":"Multi-stage: heavy builder + minimal final, COPY --from to grab only artifacts. Use scratch/distroless, static binaries (CGO_ENABLED=0), keep deps cached, don't leak the builder."},

    {"id":5,"slug":"05-volumes-storage","name":"Volumes & Persistent Storage","category":"Runtime","level":"Intermediate",
     "summary":"Persist data outside the container's ephemeral layer.",
     "keywords":["volume","bind mount","tmpfs","persistence","data"],"refs":["Manage data in Docker"],
     "overview":"Containers are ephemeral — the writable layer is lost on removal. **Volumes** (Docker-managed) and **bind mounts** (host paths) persist and share data. Volumes are the preferred way to store databases, uploads, and state.",
     "concepts":["Named volumes are managed by Docker (best for prod data).","Bind mounts map a host path (great for dev live-reload).","tmpfs mounts keep data in memory only.","Volumes survive container removal until explicitly deleted.","Mounts can be shared across containers."],
     "code":[("bash","docker volume create pgdata\ndocker run -d --name db -v pgdata:/var/lib/postgresql/data postgres:16\n# bind mount for dev:\ndocker run -v $(pwd):/app node:20 npm run dev")],
     "example":{"title":"Backup a volume","text":"Tar a volume's contents via a helper container:","code":[("bash","docker run --rm -v pgdata:/data -v $(pwd):/backup alpine \\\n  tar czf /backup/pgdata.tar.gz -C /data .")]},
     "best":["Use named volumes for stateful services in production.","Use bind mounts for local development.","Back up volumes regularly.","Avoid storing state in the container layer.","Document volume mount points and lifecycles."],
     "pitfalls":["Relying on the writable layer for important data (lost on rm).","Bind-mount path/permission mismatches across OSes.","Anonymous volumes accumulating unnoticed.","Sharing a volume between containers that both write (corruption).","Assuming docker rm deletes named volumes (it doesn't without -v).","Performance issues with bind mounts on macOS/Windows."],
     "interview":[("Volume vs bind mount?","Volumes are Docker-managed (portable, prod data); bind mounts map a specific host path (dev)."),("What happens to the writable layer on rm?","It's deleted — unsaved data is lost; persist with volumes."),("How to share data between containers?","Mount the same named volume in each."),("tmpfs mount use case?","Sensitive or scratch data kept only in memory, never on disk."),("Does docker rm delete volumes?","No — named volumes persist; use docker volume rm or docker rm -v for anonymous ones."),("Why are bind mounts slower on macOS?","File events cross the VM boundary; use volumes or cached mounts."),("How to back up a volume?","Run a throwaway container that tars the volume to a host bind mount."),("Where should a database store data?","In a named volume, not the container layer.")],
     "practice":["Run Postgres with a named volume and verify persistence.","Bind-mount source for live reload in dev.","Back up and restore a volume with tar."],
     "revision":"Container layer is ephemeral; persist with volumes (managed, prod) or bind mounts (host, dev). Volumes survive rm, can be shared/backed up; don't store state in the container layer."},

    {"id":6,"slug":"06-networking","name":"Container Networking","category":"Runtime","level":"Intermediate",
     "summary":"Bridge, host, and user-defined networks; DNS by name.",
     "keywords":["network","bridge","port","dns","publish"],"refs":["Networking overview"],
     "overview":"Docker gives each container a network namespace. The default **bridge** network isolates containers; **user-defined bridge** networks add automatic **DNS by container name**. Publish ports (`-p`) to expose services to the host.",
     "concepts":["Default bridge: containers reach each other only by IP.","User-defined bridge: built-in DNS — reach peers by name.","-p host:container publishes a port to the host.","host network shares the host's stack (Linux, no isolation).","none disables networking."],
     "code":[("bash","docker network create appnet\ndocker run -d --name db --network appnet postgres:16\ndocker run -d --name api --network appnet -p 8000:8000 myapi\n# api connects to 'db:5432' by name")],
     "example":{"title":"Publish vs expose","text":"-p maps to the host; EXPOSE only documents the port:","code":[("bash","docker run -p 8080:80 nginx   # host:8080 -> container:80")]},
     "best":["Use user-defined networks for service-to-service DNS.","Publish only the ports you need.","Reference services by name, not hardcoded IPs.","Segment networks to limit blast radius.","Bind sensitive ports to 127.0.0.1 when host-only."],
     "pitfalls":["Relying on container IPs (they change on restart).","Expecting name DNS on the default bridge (only user-defined).","Publishing everything to 0.0.0.0 unintentionally.","Port conflicts on the host.","host networking breaking port isolation.","Forgetting both containers must share the network."],
     "interview":[("Default bridge vs user-defined bridge?","User-defined networks provide automatic DNS resolution by container name; the default bridge does not."),("What does -p do?","Publishes a container port to a host port (host:container)."),("EXPOSE vs publish?","EXPOSE documents the intended port; -p actually maps it to the host."),("How do containers talk to each other?","On a shared user-defined network, by container/service name via embedded DNS."),("Why avoid hardcoding container IPs?","They change across restarts; use DNS names."),("What is host networking?","The container shares the host's network stack — no isolation, no port mapping (Linux)."),("How to restrict exposure to localhost?","Publish as 127.0.0.1:port:port."),("How to isolate tiers?","Put them on separate user-defined networks and connect only where needed.")],
     "practice":["Create a network and connect API+DB, resolving by name.","Publish a web port and curl it from the host.","Bind a port to localhost only."],
     "revision":"User-defined bridge networks give name-based DNS; default bridge needs IPs. Publish ports with -p, reference services by name, segment networks, expose only what's needed."},

    {"id":7,"slug":"07-compose","name":"Docker Compose","category":"Compose & Orchestration","level":"Intermediate",
     "summary":"Define and run multi-container apps with one YAML file.",
     "keywords":["compose","yaml","services","depends_on","up"],"refs":["Compose docs"],
     "overview":"**Docker Compose** declares a multi-service app (web, db, cache) in `compose.yaml` and manages it with `docker compose up`. It wires a shared network, named volumes, environment, and dependency order — ideal for local dev and simple deployments.",
     "concepts":["Each service maps to an image/build, ports, env, volumes.","Compose creates a default network; services resolve by name.","depends_on orders startup (not readiness — add healthchecks).","Profiles/overrides vary config per environment.","compose up/down/logs/ps manage the stack."],
     "code":[("yaml","services:\n  web:\n    build: .\n    ports: [\"8000:8000\"]\n    environment:\n      DATABASE_URL: postgres://db:5432/app\n    depends_on:\n      db:\n        condition: service_healthy\n  db:\n    image: postgres:16\n    volumes: [\"pgdata:/var/lib/postgresql/data\"]\n    healthcheck:\n      test: [\"CMD\", \"pg_isready\", \"-U\", \"postgres\"]\nvolumes:\n  pgdata:")],
     "example":{"title":"Bring it up","text":"Start, view, and tear down:","code":[("bash","docker compose up -d\ndocker compose logs -f web\ndocker compose down        # add -v to also remove volumes")]},
     "best":["Use healthchecks + depends_on condition for real readiness.","Keep secrets in env files / secrets, not the YAML.","Pin image versions.","Use named volumes for stateful services.","Separate dev/prod with overrides or profiles."],
     "pitfalls":["Assuming depends_on waits for readiness (it waits for start).","Committing secrets in compose.yaml.","Port clashes across services.","Relying on Compose for large-scale prod orchestration (use Kubernetes).","Anonymous volumes losing data.","Editing running config without recreating services."],
     "interview":[("What does Compose solve?","Declaring and running multi-container apps with networking, volumes, and env in one file."),("Does depends_on guarantee readiness?","No — only start order; combine with healthchecks and condition: service_healthy."),("How do services discover each other?","By service name over the Compose-created network's DNS."),("Compose vs Kubernetes?","Compose is great for local/simple multi-container apps; Kubernetes handles scale, self-healing, and clusters."),("How to persist DB data in Compose?","Mount a named volume to the data directory."),("How to manage env per environment?","Override files (compose.override.yaml) or profiles + .env files."),("How to view/follow logs?","docker compose logs -f <service>."),("What does compose down -v do?","Stops/removes containers, networks, and named volumes.")],
     "practice":["Write a compose.yaml for web+db with a healthcheck.","Use depends_on condition for ordered startup.","Add a Redis cache service on the same network."],
     "revision":"Compose declares multi-service apps in YAML (network/volumes/env). Use healthchecks for readiness, named volumes for state, secrets via env files; it's for dev/simple prod — Kubernetes for scale."},

    {"id":8,"slug":"08-security","name":"Container Security","category":"Production","level":"Advanced",
     "summary":"Reduce attack surface: non-root, minimal images, scanning.",
     "keywords":["security","non-root","scan","secrets","capabilities"],"refs":["Docker security"],
     "overview":"Containers share the host kernel, so security hardening matters: run as **non-root**, use **minimal images**, drop Linux **capabilities**, scan for CVEs, and never bake **secrets** into images. Defense in depth limits the blast radius of a compromise.",
     "concepts":["Run as a non-root USER; drop unneeded capabilities.","Minimal base images (distroless/alpine) shrink CVE surface.","Scan images (Trivy/Grype) in CI.","Secrets via runtime env/secret stores, not image layers.","Read-only root filesystem + no-new-privileges hardening."],
     "code":[("bash","docker run --read-only --cap-drop ALL --security-opt no-new-privileges \\\n  -u 1000:1000 myapp:1.0\n# scan for vulnerabilities\ntrivy image myapp:1.0")],
     "example":{"title":"Non-root in Dockerfile","text":"Create and switch to an unprivileged user:","code":[("dockerfile","RUN adduser -D -u 1000 app\nUSER app")]},
     "best":["Always run as a non-root user.","Drop all capabilities and add back only what's needed.","Scan images in CI and patch base images regularly.","Use read-only filesystems and no-new-privileges.","Inject secrets at runtime (env/secret manager), never in layers."],
     "pitfalls":["Running as root (default).","Secrets baked into image history.","Fat images full of unused, vulnerable packages.","Granting --privileged casually.","Never updating base images (stale CVEs).","Mounting the Docker socket into containers (host takeover)."],
     "interview":[("Why run containers as non-root?","To limit damage if the app is compromised — root in container can be closer to host root."),("How to shrink attack surface?","Minimal/distroless base, drop capabilities, remove unused packages, read-only FS."),("Where should secrets live?","Runtime env vars or a secret manager — never baked into image layers/history."),("Why scan images?","Base images and deps accrue CVEs; scanners (Trivy) catch known vulnerabilities."),("Risk of mounting the Docker socket?","It grants control of the daemon — effectively host root."),("What does --cap-drop ALL do?","Removes all Linux capabilities; add back only required ones."),("Is container isolation a security boundary?","Weaker than VMs (shared kernel); for hostile multi-tenancy add VMs/gVisor."),("no-new-privileges flag?","Prevents processes from gaining new privileges via setuid binaries.")],
     "practice":["Make a Dockerfile run as non-root.","Run a container with --read-only and --cap-drop ALL.","Scan an image with Trivy and fix a finding."],
     "revision":"Harden containers: non-root USER, minimal images, drop capabilities, read-only FS, no-new-privileges, scan in CI, secrets at runtime. Never mount the Docker socket or run --privileged casually."},

    {"id":9,"slug":"09-image-optimization","name":"Image Size & Build Optimization","category":"Production","level":"Advanced",
     "summary":"Smaller, faster, cache-friendly images.",
     "keywords":["optimization","size","cache","layers","buildkit"],"refs":["BuildKit"],
     "overview":"Smaller images pull faster, deploy quicker, and have less attack surface. Optimize by choosing slim bases, multi-stage builds, ordering layers for cache hits, cleaning caches in the same layer, and using **BuildKit** features (cache mounts, parallelism).",
     "concepts":["Slim/distroless bases cut size dramatically.","Multi-stage builds drop build tooling from the final image.","Order layers stable→volatile for cache reuse.","Clean package caches within the same RUN.","BuildKit adds cache mounts and parallel stage builds."],
     "code":[("dockerfile","# syntax=docker/dockerfile:1\nFROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN --mount=type=cache,target=/root/.cache/pip \\\n    pip install -r requirements.txt\nCOPY . .")],
     "example":{"title":"Clean in the same layer","text":"Avoid leaving apt caches in the image:","code":[("dockerfile","RUN apt-get update && apt-get install -y --no-install-recommends curl \\\n && rm -rf /var/lib/apt/lists/*")]},
     "best":["Use slim/distroless bases and multi-stage builds.","Combine install + cleanup in one RUN.","Leverage BuildKit cache mounts for deps.","Add .dockerignore to trim context.","Pin versions for reproducible, cacheable builds."],
     "pitfalls":["Cleaning caches in a separate RUN (no size savings).","Copying source before installing deps (cache busts).","Installing recommended/unused packages.","Not enabling BuildKit.","latest base tags causing non-reproducible builds.","Large build context slowing every build."],
     "interview":[("Top ways to shrink an image?","Slim/distroless base, multi-stage builds, combined+cleaned RUN steps, .dockerignore."),("Why clean caches in the same RUN?","Each RUN is a layer; cleaning later doesn't remove bytes already committed in an earlier layer."),("What does BuildKit add?","Parallel builds, cache mounts (--mount=type=cache), secrets mounts, better caching."),("How to maximize build cache?","Order instructions stable→volatile and copy lockfiles before source."),("Why pin versions?","Reproducibility and stable cache keys."),("--no-install-recommends benefit?","Avoids pulling optional packages, reducing size and CVEs."),("How does .dockerignore help builds?","Shrinks the context sent to the daemon, speeding builds and avoiding leaks."),("Multi-stage size win?","Final image carries only artifacts, not compilers/source/caches.")],
     "practice":["Cut an image size with a slim base + cleanup.","Enable BuildKit and add a pip cache mount.","Measure build time improvement from layer reordering."],
     "revision":"Optimize images: slim/distroless + multi-stage, stable→volatile layers, clean in same RUN, BuildKit cache mounts, .dockerignore, pinned versions. Smaller = faster + safer."},

    {"id":10,"slug":"10-logging-debugging","name":"Logging, Monitoring & Debugging","category":"Production","level":"Advanced",
     "summary":"Observe and troubleshoot running containers.",
     "keywords":["logs","exec","inspect","stats","healthcheck"],"refs":["Docker logging"],
     "overview":"Containers should log to **stdout/stderr** so the platform collects them. Debug with `logs`, `exec`, `inspect`, and `stats`; define **HEALTHCHECK**s so orchestrators know readiness. Centralize logs and metrics for production observability.",
     "concepts":["Log to stdout/stderr; let the runtime/agent ship logs.","docker logs/exec/inspect/stats for inspection.","HEALTHCHECK reports container health to the platform.","Logging drivers (json-file, journald, fluentd) route logs.","One concern per container simplifies observability."],
     "code":[("bash","docker logs -f --tail 100 api\ndocker exec -it api sh         # shell into a running container\ndocker inspect api            # full config/state JSON\ndocker stats                  # live CPU/mem/IO")],
     "example":{"title":"HEALTHCHECK","text":"Let the platform detect an unhealthy container:","code":[("dockerfile","HEALTHCHECK --interval=30s --timeout=3s \\\n  CMD curl -f http://localhost:8000/health || exit 1")]},
     "best":["Write logs to stdout/stderr, not files inside the container.","Add HEALTHCHECKs for readiness/liveness.","Centralize logs (ELK/Loki) and metrics (Prometheus).","Use docker exec for live debugging, not SSH.","Set log rotation to avoid disk fill."],
     "pitfalls":["Logging to files inside the container (lost on removal).","No healthcheck → orchestrator can't detect failures.","Unbounded json-file logs filling the disk.","Installing SSH instead of using exec.","Noisy logs without levels/structure.","Debugging distroless images lacking a shell."],
     "interview":[("Where should containers log?","To stdout/stderr so the runtime and log agents can collect and ship them."),("How to debug a running container?","docker exec into it, inspect config/state, check logs and stats."),("What does HEALTHCHECK do?","Runs a command periodically; the result tells the platform if the container is healthy."),("How to prevent logs filling the disk?","Configure log rotation (max-size/max-file) or a centralized driver."),("Why not SSH into containers?","It adds attack surface and breaks immutability; use exec instead."),("How to debug a distroless image?","Use a debug image variant or an ephemeral debug sidecar/exec with tooling."),("docker inspect uses?","View full configuration, mounts, networks, env, and state as JSON."),("What is centralized logging?","Aggregating container logs into a system (ELK/Loki) for search and alerting.")],
     "practice":["Tail and follow a container's logs.","Add a HEALTHCHECK and observe status.","Use exec + stats to debug a slow container."],
     "revision":"Log to stdout/stderr, add HEALTHCHECKs, debug via logs/exec/inspect/stats, centralize logs+metrics, rotate logs. Avoid in-container log files and SSH."},
  ],
}
