# System Design Handbook spec
SPEC = {
  "id": "system-design", "name": "System Design Handbook", "icon": "🏛️",
  "tagline": "Master system design for senior/staff/FAANG interviews: scaling, caching, databases, queues, consistency, and real architectures.",
  "itemNoun": "Topic",
  "levels": ["Beginner", "Intermediate", "Advanced"],
  "categories": [
    {"name": "Fundamentals", "level": "Beginner", "icon": "📐"},
    {"name": "Scaling", "level": "Intermediate", "icon": "📈"},
    {"name": "Data", "level": "Intermediate", "icon": "🗄️"},
    {"name": "Distributed Systems", "level": "Advanced", "icon": "🌐"},
    {"name": "Practice", "level": "Advanced", "icon": "🎯"},
  ],
  "items": [
    {"id":1,"slug":"01-interview-framework","name":"The System Design Interview Framework","category":"Fundamentals","level":"Beginner",
     "summary":"A repeatable structure: requirements → estimates → API → data → design → scale.",
     "keywords":["framework","requirements","estimation","tradeoffs","interview"],"refs":["System design primer"],
     "overview":"System design interviews are open-ended; a **structured approach** signals seniority. Drive the conversation: clarify requirements, estimate scale, define the API and data model, sketch a high-level design, then deep-dive and address bottlenecks with explicit trade-offs.",
     "concepts":["Clarify functional + non-functional requirements first.","Estimate scale (QPS, storage, bandwidth) to justify choices.","Define API contract and data model before components.","Start simple, then scale and remove bottlenecks.","Every decision is a trade-off — state them aloud."],
     "code":[("text","1. Requirements   (functional + non-functional, constraints)\n2. Estimation    (QPS, storage, read:write ratio)\n3. API           (endpoints / contracts)\n4. Data model    (schema, access patterns)\n5. High-level    (boxes: client, LB, services, DB, cache, queue)\n6. Deep dive     (bottlenecks, scaling, failure modes)\n7. Wrap up       (trade-offs, future work)")],
     "example":{"title":"Non-functional requirements","text":"Pin down scale, latency, availability, consistency, and durability — these drive the architecture more than features.","code":[("text","Availability: 99.9%   Latency: p99 < 200ms\nConsistency: eventual OK?   Read:Write = 100:1   DAU: 10M")]},
     "best":["Spend real time clarifying before designing.","Quantify scale with back-of-envelope math.","State assumptions explicitly.","Begin simple; scale only where numbers demand.","Always articulate trade-offs and alternatives."],
     "pitfalls":["Jumping to components before requirements.","Skipping capacity estimation.","Designing for hypothetical scale you didn't justify.","Silent decisions with no trade-off discussion.","Ignoring non-functional requirements.","Getting lost in one component and missing the big picture."],
     "interview":[("How do you start a system design question?","Clarify functional and non-functional requirements and constraints before any design."),("Why estimate scale?","Numbers (QPS, storage, read:write) justify architectural choices like caching, sharding, and replication."),("What are non-functional requirements?","Availability, latency, consistency, durability, scalability — they shape the design most."),("How to structure the whole answer?","Requirements → estimation → API → data → high-level → deep dive → trade-offs."),("Why start simple?","Avoid over-engineering; add complexity only where the scale requires it."),("How to show seniority?","Drive the conversation, quantify, and discuss trade-offs and failure modes."),("What if requirements are ambiguous?","Ask targeted questions and state assumptions explicitly."),("How to manage time?","Time-box: requirements/estimation early, then high-level, then one or two deep dives.")],
     "practice":["Run the 7-step framework on 'design a URL shortener'.","List non-functional requirements for a chat app.","Do capacity estimation for 10M DAU."],
     "revision":"Framework: requirements → estimation → API → data model → high-level → deep dive → trade-offs. Quantify scale, start simple, state assumptions and trade-offs aloud."},

    {"id":2,"slug":"02-capacity-estimation","name":"Back-of-the-Envelope Estimation","category":"Fundamentals","level":"Beginner",
     "summary":"Quick math for QPS, storage, and bandwidth to size systems.",
     "keywords":["estimation","qps","storage","bandwidth","latency numbers"],"refs":["Latency numbers every programmer should know"],
     "overview":"Estimation turns vague scale into concrete numbers that justify design choices. Compute **QPS** (writes & reads), **storage** growth, and **bandwidth** from user counts and per-item sizes, and reason with the classic latency numbers (memory ns, SSD μs, network ms).",
     "concepts":["QPS = daily actions / 86,400 (×peak factor 2–10).","Storage = items/day × size × retention.","Read:write ratios drive caching/replication.","Latency hierarchy: RAM ns « SSD μs « network ms.","Round aggressively; the order of magnitude matters."],
     "code":[("text","Example: 10M DAU, 1 post each, read:write = 100:1\nwrites/day = 10M  -> ~115 wps avg, ~1000 wps peak\nreads      = 1B/day -> ~11.5k rps avg, ~100k rps peak  (need caching!)\nstorage    = 10M * 1KB * 365 = ~3.65 TB/year")],
     "example":{"title":"Latency intuition","text":"Memory reference ~100ns, SSD ~100μs, network round trip within DC ~0.5ms, cross-region ~100ms — design to keep hot paths in memory.","code":[("text","L1 ~1ns | RAM ~100ns | SSD ~100us | DC RTT ~0.5ms | Cross-region ~100ms")]},
     "best":["Do the math out loud; it justifies decisions.","Apply a peak multiplier over averages.","Round to powers of ten.","Separate read and write paths.","Use latency numbers to choose storage tiers."],
     "pitfalls":["Skipping estimation entirely.","Using averages without peak factors.","Over-precise figures (false confidence).","Ignoring storage growth/retention.","Forgetting metadata/replication overhead.","Treating reads and writes as one number."],
     "interview":[("How do you estimate QPS?","Daily actions ÷ 86,400, then multiply by a peak factor (2–10×)."),("Why separate reads and writes?","They scale differently; high read:write ratios point to caching/replicas."),("Key latency numbers?","RAM ~100ns, SSD ~100μs, intra-DC RTT ~0.5ms, cross-region ~100ms."),("How to estimate storage?","items/day × item size × retention period × replication factor."),("Why round aggressively?","Order of magnitude drives the design; precision is false confidence."),("What's a peak factor?","A multiplier capturing traffic spikes over the average."),("How does estimation drive design?","High QPS → caching/sharding; large storage → partitioning; tight latency → in-memory."),("Bandwidth estimation?","QPS × payload size, both ingress and egress.")],
     "practice":["Estimate QPS for a 50M-DAU feed.","Compute yearly storage for image uploads.","Use latency numbers to justify a cache."],
     "revision":"Estimate QPS (daily/86.4k × peak), storage (items×size×retention×replication), bandwidth (QPS×size). Use latency hierarchy (RAM≪SSD≪network). Round to orders of magnitude; split reads/writes."},

    {"id":3,"slug":"03-load-balancing","name":"Load Balancing","category":"Scaling","level":"Intermediate",
     "summary":"Distribute traffic across servers for scale and availability.",
     "keywords":["load balancer","round robin","health check","l4 l7","sticky"],"refs":["Load balancing"],
     "overview":"A **load balancer** spreads requests across many servers, enabling horizontal scaling and fault tolerance. **L4** balances by IP/port (fast); **L7** routes by HTTP content (smart). Health checks remove unhealthy instances automatically.",
     "concepts":["Algorithms: round-robin, least-connections, IP/consistent hash.","L4 (transport) vs L7 (application) load balancing.","Health checks drain failed nodes.","Sticky sessions pin a client to a server (avoid if stateless).","LBs themselves need redundancy (no single point of failure)."],
     "code":[("text","        ┌──────────────┐\nclients →│ Load Balancer│→ [app-1]\n        │  (L7, health │→ [app-2]\n        │   checks)    │→ [app-3]\n        └──────────────┘   (auto-remove unhealthy)")],
     "example":{"title":"Least connections","text":"Route to the server with the fewest active connections — better than round-robin under uneven request durations.","code":[("text","RR:   evenly, ignores load\nLC:   picks least-busy node (good for long requests)\nHash: same client -> same node (cache affinity)")]},
     "best":["Keep app servers stateless so any node can serve any request.","Use health checks to auto-remove bad nodes.","Pick the algorithm for your traffic (LC for uneven durations).","Run redundant LBs (active-active/standby).","Terminate TLS at the LB."],
     "pitfalls":["Sticky sessions creating stateful bottlenecks.","Single load balancer as a SPOF.","No health checks → routing to dead servers.","Ignoring connection draining on deploys.","Round-robin under highly variable request costs.","LB becoming the capacity ceiling."],
     "interview":[("Why load balance?","To scale horizontally and tolerate server failures by distributing traffic."),("L4 vs L7?","L4 routes by IP/port (fast, protocol-agnostic); L7 routes by HTTP content (path/host/cookies)."),("Common algorithms?","Round-robin, least-connections, IP/consistent hashing."),("Why prefer stateless servers?","Any server can handle any request, enabling easy scaling and failover."),("What are health checks?","Periodic probes that remove unhealthy instances from rotation."),("Downside of sticky sessions?","They reintroduce state/affinity, harming balance and failover."),("Is the LB a SPOF?","It can be — run redundant LBs and use DNS/anycast failover."),("What is connection draining?","Letting in-flight requests finish before removing a node during deploys.")],
     "practice":["Diagram an L7 LB fronting 3 stateless app servers.","Choose an algorithm for long-lived connections.","Explain how to make the LB itself HA."],
     "revision":"LBs distribute traffic (RR/least-conn/hash), L4 (fast) vs L7 (smart), with health checks + draining. Keep servers stateless; avoid sticky sessions; make the LB redundant."},

    {"id":4,"slug":"04-caching","name":"Caching","category":"Scaling","level":"Intermediate",
     "summary":"Serve hot data from fast memory to cut latency and load.",
     "keywords":["cache","redis","ttl","eviction","invalidation","cdn"],"refs":["Caching strategies"],
     "overview":"Caching stores frequently-accessed data in fast storage (memory/Redis/CDN) to reduce latency and backend load. The hard parts are **invalidation** (keeping caches fresh) and choosing a **write strategy** and **eviction policy**.",
     "concepts":["Patterns: cache-aside (lazy), write-through, write-back.","Eviction: LRU, LFU, TTL expiry.","CDN caches static assets near users (edge).","Invalidation keeps cache consistent with source.","Watch for thundering herd / cache stampede."],
     "code":[("text","cache-aside read:\n  v = cache.get(k)\n  if v is None:\n     v = db.get(k); cache.set(k, v, ttl)\n  return v")],
     "example":{"title":"Stampede protection","text":"On a cache miss for a hot key, use a lock or 'request coalescing' so only one request rebuilds it.","code":[("text","miss -> acquire lock -> recompute -> set -> release\nothers wait or serve stale briefly")]},
     "best":["Cache hot, expensive, read-heavy data with TTLs.","Use cache-aside as a sensible default.","Place a CDN in front of static content.","Plan invalidation on writes (delete/update keys).","Protect against stampedes (locks, jitter, stale-while-revalidate)."],
     "pitfalls":["Stale data from missing invalidation.","Caching everything (low hit ratio, wasted memory).","Thundering herd on popular-key expiry.","No TTL → unbounded growth/staleness.","Caching per-user data under shared keys.","Treating the cache as the source of truth."],
     "interview":[("Why cache?","To reduce latency and backend load by serving hot data from fast memory."),("Cache-aside vs write-through vs write-back?","Lazy fill on miss / write to cache+DB synchronously / write to cache then async to DB (fast but risk loss)."),("Eviction policies?","LRU/LFU/TTL decide what to drop when full or stale."),("Hardest caching problem?","Invalidation — keeping cached data consistent with the source."),("What is a cache stampede?","Many requests recomputing the same expired hot key at once; mitigate with locks/jitter/stale serving."),("Where does a CDN fit?","Caching static/edge content close to users to cut latency and origin load."),("How to size a cache?","From the working set and hit-ratio targets; monitor hit rate."),("Risk of write-back?","Data loss if the cache fails before flushing to the DB.")],
     "practice":["Implement cache-aside with TTL for a hot query.","Add stampede protection for a popular key.","Decide what to cache vs not for a feed."],
     "revision":"Cache hot read-heavy data (cache-aside default; write-through/back options), evict via LRU/LFU/TTL, CDN for static. Hard part = invalidation; guard against stampedes; never treat cache as source of truth."},

    {"id":5,"slug":"05-database-scaling","name":"Database Scaling: Replication & Sharding","category":"Data","level":"Intermediate",
     "summary":"Scale reads with replicas; scale writes/storage with sharding.",
     "keywords":["replication","sharding","partition","read replica","leader follower"],"refs":["Database scaling"],
     "overview":"Scale databases by **replication** (copy data to followers for read scaling and HA) and **sharding/partitioning** (split data across nodes for write/storage scaling). Replication brings replication lag; sharding brings cross-shard query complexity.",
     "concepts":["Leader-follower: writes to leader, reads from replicas.","Replication lag → followers may serve stale reads.","Sharding splits data by key (range/hash/consistent hash).","Choose a shard key that spreads load evenly.","Cross-shard joins/transactions are hard — avoid or use scatter-gather."],
     "code":[("text","Writes ─▶ [Leader] ──replicate──▶ [Follower-1] ◀─ reads\n                       └─────────▶ [Follower-2] ◀─ reads\n\nSharding (hash(user_id)%N): user data lives on exactly one shard")],
     "example":{"title":"Choosing a shard key","text":"A bad key (e.g., country) creates hotspots; hashing the user id spreads writes evenly but makes range scans expensive.","code":[("text","hash(user_id) -> even spread, no range scans\nrange by date  -> good for time queries, hot 'today' shard")]},
     "best":["Use read replicas to scale read-heavy workloads.","Pick a shard key that distributes load and matches access patterns.","Prefer single-shard transactions.","Add caching before sharding when possible.","Plan for resharding/rebalancing from day one."],
     "pitfalls":["Reading from replicas and seeing stale data unexpectedly.","Hot shards from a skewed shard key.","Cross-shard transactions/joins killing performance.","Resharding without a migration plan.","Treating replicas as a write-scaling solution (they aren't).","Choosing a shard key you can't change later."],
     "interview":[("Replication vs sharding?","Replication copies data (read scaling + HA); sharding splits data (write/storage scaling)."),("What is replication lag?","Delay before followers reflect the leader's writes, causing potentially stale reads."),("How to choose a shard key?","One that spreads load evenly and aligns with common queries to avoid scatter-gather."),("Why are cross-shard transactions hard?","They need distributed coordination (2PC/sagas), increasing latency and failure modes."),("How to scale writes?","Shard/partition, batch, and use write-optimized stores; replicas don't help writes."),("What causes hot shards?","Skewed keys concentrating traffic; fix with better hashing or splitting."),("Consistent hashing benefit?","Minimal data movement when nodes are added/removed."),("Read-after-write consistency on replicas?","Route those reads to the leader or use sticky/quorum reads.")],
     "practice":["Design leader-follower for a read-heavy app.","Pick and justify a shard key for messages.","Explain how to reshard safely."],
     "revision":"Replication (followers) scales reads + HA but adds lag; sharding splits data for write/storage scale but complicates cross-shard ops. Pick a balanced shard key; prefer single-shard transactions; cache first."},

    {"id":6,"slug":"06-sql-vs-nosql","name":"SQL vs NoSQL & Data Modeling","category":"Data","level":"Intermediate",
     "summary":"Choose a datastore by access patterns and consistency needs.",
     "keywords":["sql","nosql","document","key-value","wide-column","acid"],"refs":["Database types"],
     "overview":"Pick storage by **access pattern**, not hype. **SQL** (relational, ACID, joins) fits structured, transactional data; **NoSQL** families — key-value, document, wide-column, graph — trade joins/strict schema for scale and flexible models. Often you combine several (polyglot persistence).",
     "concepts":["SQL: schema, joins, strong ACID transactions.","Key-value (Redis/DynamoDB): fast lookups by key.","Document (MongoDB): flexible nested JSON.","Wide-column (Cassandra): huge write throughput, partitioned.","Model NoSQL around queries (denormalize for reads)."],
     "code":[("text","Need joins + transactions?        -> SQL (Postgres/MySQL)\nSimple key lookups, low latency?  -> Key-Value (Redis/Dynamo)\nFlexible nested docs?             -> Document (MongoDB)\nMassive writes, time-series?      -> Wide-Column (Cassandra)\nRelationships/traversal?          -> Graph (Neo4j)")],
     "example":{"title":"Model for queries (NoSQL)","text":"In Cassandra you design tables per query and denormalize, since joins aren't available.","code":[("text","Query: messages by chat, newest first\nTable PK = chat_id, clustering = ts DESC  (one table per access pattern)")]},
     "best":["Choose based on access patterns and consistency needs.","Use SQL by default for transactional, relational data.","Denormalize NoSQL around read queries.","Consider polyglot persistence (right tool per job).","Know your consistency requirements before choosing."],
     "pitfalls":["Picking NoSQL for inherently relational/transactional data.","Expecting joins/ACID from eventually-consistent stores.","Modeling NoSQL like a relational schema.","Ignoring hot partitions in wide-column stores.","Premature NoSQL adoption for scale you don't have.","Underestimating operational complexity of multiple stores."],
     "interview":[("How to choose SQL vs NoSQL?","By access patterns, consistency, and scale — SQL for relational/transactional, NoSQL for scale/flexibility."),("NoSQL families?","Key-value, document, wide-column, graph — each suited to different access patterns."),("When is SQL the right default?","Structured data needing joins and strong transactions."),("How do you model NoSQL data?","Around queries: denormalize and create tables/collections per access pattern."),("Trade-off of NoSQL scale?","Often weaker consistency and no joins/ACID across entities."),("What is polyglot persistence?","Using multiple datastores, each for the workload it fits best."),("Hot partition problem?","Skewed partition keys overload one node; design keys for even distribution."),("Can NoSQL be ACID?","Some offer transactions (e.g., DynamoDB, MongoDB) but with constraints.")],
     "practice":["Choose a datastore for a shopping cart and justify it.","Model a chat-by-room table for a wide-column store.","Argue SQL vs NoSQL for an analytics workload."],
     "revision":"Choose by access pattern: SQL (relational/ACID/joins) vs NoSQL (key-value/document/wide-column/graph) for scale/flexibility. Model NoSQL around queries; polyglot persistence is common; know consistency needs."},

    {"id":7,"slug":"07-message-queues","name":"Message Queues & Async Processing","category":"Distributed Systems","level":"Advanced",
     "summary":"Decouple services with async messaging for resilience and scale.",
     "keywords":["queue","kafka","pub-sub","async","backpressure","delivery"],"refs":["Messaging systems"],
     "overview":"**Message queues** (RabbitMQ, SQS) and **logs** (Kafka) decouple producers from consumers, smoothing spikes, enabling async work, and improving resilience. They introduce delivery semantics (at-least/at-most/exactly-once) and the need for **idempotent** consumers.",
     "concepts":["Decoupling: producers don't wait on consumers.","Queue (work distribution) vs pub-sub/log (fan-out, replay).","Delivery: at-least-once (dedupe needed), at-most-once, exactly-once (hard).","Backpressure & buffering absorb spikes.","Dead-letter queues capture poison messages."],
     "code":[("text","Producer ─▶ [ Queue / Kafka topic ] ─▶ Consumer group\n                         (buffer spikes, retry, DLQ)\nWeb request -> enqueue 'send_email' -> worker processes later")],
     "example":{"title":"Idempotent consumer","text":"At-least-once delivery means duplicates; make handlers idempotent (dedupe by message id).","code":[("text","on message(id, payload):\n  if seen(id): return        # dedupe\n  process(payload); mark_seen(id)")]},
     "best":["Offload slow/spiky work to async workers.","Design consumers to be idempotent.","Use dead-letter queues for failures.","Pick Kafka for high-throughput streams/replay; queues for task distribution.","Monitor lag/queue depth."],
     "pitfalls":["Assuming exactly-once without idempotency.","Unbounded queues hiding a slow consumer.","No DLQ → poison messages block progress.","Ordering assumptions across partitions.","Tight producer-consumer coupling defeating the purpose.","Ignoring consumer lag growth."],
     "interview":[("Why use a message queue?","To decouple services, absorb spikes, and process work asynchronously and resiliently."),("Queue vs pub-sub/log?","Queues distribute work to one consumer; pub/sub and logs fan out to many and allow replay (Kafka)."),("Delivery semantics?","At-least-once (duplicates possible), at-most-once (loss possible), exactly-once (hard, usually idempotency + dedupe)."),("Why idempotent consumers?","At-least-once delivery causes duplicates; idempotency makes reprocessing safe."),("What is a DLQ?","A dead-letter queue holding messages that repeatedly fail, for inspection/retry."),("Kafka vs RabbitMQ?","Kafka is a partitioned, replayable log for high-throughput streaming; RabbitMQ is a flexible broker for task queues."),("What is backpressure?","Signaling/buffering to prevent fast producers overwhelming consumers."),("How to preserve ordering?","Within a partition/queue; global ordering across partitions isn't guaranteed.")],
     "practice":["Design async email sending with a queue + worker.","Make a consumer idempotent via message ids.","Add a DLQ and explain when messages land there."],
     "revision":"Queues/logs decouple producers/consumers, smooth spikes, enable async. Mind delivery semantics (at-least-once → idempotent consumers), use DLQs, Kafka for streams/replay; monitor lag."},

    {"id":8,"slug":"08-cap-consistency","name":"CAP, Consistency & Replication","category":"Distributed Systems","level":"Advanced",
     "summary":"Trade consistency vs availability under network partitions.",
     "keywords":["cap","consistency","availability","partition","quorum","eventual"],"refs":["CAP theorem"],
     "overview":"The **CAP theorem** says under a network **partition** you must choose **consistency** (reject stale) or **availability** (serve possibly-stale). Real systems tune this via **quorums** and offer a spectrum from strong to **eventual** consistency.",
     "concepts":["During a partition: pick CP (consistent) or AP (available).","Strong vs eventual consistency are endpoints of a spectrum.","Quorums: R + W > N gives strong reads.","Tunable consistency (Dynamo/Cassandra) per request.","PACELC extends CAP with latency vs consistency when no partition."],
     "code":[("text","CAP under partition:\n  CP -> reject to stay consistent (e.g., spanner, zookeeper)\n  AP -> serve stale to stay available (e.g., dynamo, cassandra)\n\nQuorum: N replicas, write to W, read from R; R + W > N => strong")],
     "example":{"title":"Quorum reads/writes","text":"With N=3, W=2, R=2: R+W=4>3, so a read always overlaps the latest write → strong consistency.","code":[("text","N=3, W=2, R=2 -> R+W>N -> consistent\nN=3, W=1, R=1 -> fast but eventually consistent")]},
     "best":["Match consistency level to the use case (money=strong, likes=eventual).","Use quorums to tune the consistency/latency trade-off.","Make eventual-consistency UX-aware (read-your-writes where needed).","State CAP choice explicitly in designs.","Consider PACELC (latency even without partitions)."],
     "pitfalls":["Claiming a system is 'CA' (partitions are unavoidable).","Assuming eventual consistency is fine for all data.","Ignoring read-your-writes expectations.","Misconfigured quorums giving neither speed nor consistency.","Conflating consistency models with isolation levels.","Overpaying for strong consistency where eventual suffices."],
     "interview":[("State the CAP theorem.","Under a network partition, a system must choose between consistency and availability; you can't have both then."),("Is 'CA' achievable?","Not meaningfully — partitions happen, so you really choose CP or AP under them."),("Strong vs eventual consistency?","Strong returns the latest write always; eventual converges over time, allowing temporary staleness."),("How do quorums work?","With N replicas, requiring R+W>N guarantees read/write overlap for strong consistency."),("When is eventual consistency acceptable?","For non-critical, high-availability data like counts, feeds, presence."),("What is PACELC?","Else (no partition) you still trade Latency vs Consistency."),("Read-your-writes consistency?","A user always sees their own latest write; route to leader or use session guarantees."),("Money transfer consistency?","Strong/transactional — staleness is unacceptable.")],
     "practice":["Decide CP vs AP for a banking ledger vs a like counter.","Configure N/R/W for strong vs fast reads.","Explain read-your-writes for a profile update."],
     "revision":"CAP: under partition choose C or A. Consistency is a spectrum (strong↔eventual); quorums (R+W>N) tune it. Strong for money, eventual for feeds; remember PACELC (latency vs consistency otherwise)."},

    {"id":9,"slug":"09-microservices","name":"Microservices vs Monolith","category":"Distributed Systems","level":"Advanced",
     "summary":"Trade deployment independence for distributed complexity.",
     "keywords":["microservices","monolith","api gateway","service mesh","bounded context"],"refs":["Microservices"],
     "overview":"A **monolith** is one deployable unit (simple, fast to start). **Microservices** split by business capability for independent scaling/deploys, at the cost of distributed-systems complexity (network calls, data consistency, observability). Most systems should start monolithic and split when pain justifies it.",
     "concepts":["Split by bounded context / business capability.","Each service owns its data (no shared DB).","API gateway handles routing/auth/rate-limiting.","Service mesh manages service-to-service traffic.","Distributed transactions → sagas, not 2PC across services."],
     "code":[("text","Client ─▶ API Gateway ─▶ [Auth] [Orders] [Payments] [Inventory]\n                          each: own DB, own deploy, own scaling")],
     "example":{"title":"Saga over distributed transaction","text":"Coordinate a multi-service workflow with compensating actions instead of a global transaction.","code":[("text","CreateOrder -> ReserveInventory -> ChargePayment\n  on failure: compensate (refund, release inventory)")]},
     "best":["Start monolithic; extract services when boundaries are clear.","Give each service its own data store.","Use an API gateway for cross-cutting concerns.","Design for failure (timeouts, retries, circuit breakers).","Invest in observability (tracing, metrics, logs)."],
     "pitfalls":["Premature microservices (distributed monolith).","Shared database coupling services.","Synchronous call chains amplifying latency/failure.","Distributed transactions via 2PC across services.","No tracing → undebuggable failures.","Network calls treated as if local (no timeouts/retries)."],
     "interview":[("Monolith vs microservices?","Monolith: one deployable, simple; microservices: independent deploy/scale per capability, with distributed complexity."),("When to use microservices?","When clear bounded contexts and team/scale needs justify the operational cost."),("Why own-database-per-service?","To decouple services; a shared DB recreates a tightly-coupled monolith."),("How to handle cross-service transactions?","Sagas with compensating actions, not global 2PC."),("Role of an API gateway?","Single entry point for routing, auth, rate-limiting, and aggregation."),("What's a distributed monolith?","Microservices so coupled they must deploy together — worst of both worlds."),("How to manage service-to-service comms?","Timeouts, retries with backoff, circuit breakers, and a service mesh."),("Why is observability critical?","Failures span services; distributed tracing is needed to debug them.")],
     "practice":["Decide monolith vs microservices for an MVP.","Design a saga for order→payment→inventory.","Add resilience (timeouts/circuit breaker) to a call."],
     "revision":"Monolith (simple) vs microservices (independent deploy/scale + distributed complexity). Split by bounded context, DB-per-service, API gateway, sagas not 2PC, design for failure, invest in observability. Start monolith."},

    {"id":10,"slug":"10-rate-limiting","name":"Rate Limiting & Resilience","category":"Distributed Systems","level":"Advanced",
     "summary":"Protect systems with limits, timeouts, and circuit breakers.",
     "keywords":["rate limit","token bucket","circuit breaker","retry","backoff"],"refs":["Rate limiting algorithms"],
     "overview":"Resilience patterns keep systems stable under load and partial failure: **rate limiting** (token/leaky bucket, sliding window) caps request rates; **timeouts, retries with backoff, and circuit breakers** prevent cascading failures and resource exhaustion.",
     "concepts":["Token bucket allows bursts up to a capacity, refilled at a rate.","Sliding window smooths fixed-window edge bursts.","Distributed rate limiting uses a shared store (Redis).","Timeouts + retries (with jittered backoff) handle transient failures.","Circuit breakers stop calling a failing dependency."],
     "code":[("text","Token bucket: capacity C, refill R/sec\n  on request: if tokens>0: tokens--; allow  else: reject(429)\nCircuit breaker: closed -> (failures>threshold) -> open -> half-open -> closed")],
     "example":{"title":"Retry with backoff + jitter","text":"Avoid retry storms by spacing retries with exponential backoff and randomness.","code":[("text","attempt n: wait = min(cap, base * 2^n) + random_jitter")]},
     "best":["Rate-limit per user/IP/key to prevent abuse.","Use token bucket for burst tolerance.","Always set timeouts on remote calls.","Retry idempotent ops with exponential backoff + jitter.","Add circuit breakers around flaky dependencies."],
     "pitfalls":["No timeouts → threads pile up on a slow dependency.","Retrying non-idempotent operations (duplicate effects).","Retry storms without backoff/jitter (thundering herd).","Fixed-window limiting allowing edge bursts.","Local-only limits in a distributed fleet.","No circuit breaker → cascading failures."],
     "interview":[("Common rate-limiting algorithms?","Token bucket, leaky bucket, fixed window, sliding window (log/counter)."),("Token vs leaky bucket?","Token bucket allows bursts up to capacity; leaky bucket enforces a steady output rate."),("Why timeouts on remote calls?","To free resources and avoid pile-ups when a dependency is slow."),("How to retry safely?","Only idempotent operations, with exponential backoff and jitter, bounded attempts."),("What does a circuit breaker do?","Stops calling a failing service after a threshold, allowing it to recover; probes via half-open."),("Distributed rate limiting?","Coordinate counts in a shared store (e.g., Redis) across instances."),("Fixed vs sliding window?","Sliding window avoids the double-burst at fixed-window boundaries."),("How to prevent retry storms?","Backoff + jitter and circuit breakers.")],
     "practice":["Implement token-bucket limiting logic.","Add timeout + backoff retry to an API call.","Describe a circuit breaker's state transitions."],
     "revision":"Resilience: rate limit (token/leaky bucket, sliding window; distributed via Redis), timeouts, idempotent retries with backoff+jitter, circuit breakers. Prevent pile-ups, retry storms, and cascading failure."},

    {"id":11,"slug":"11-design-url-shortener","name":"Design: URL Shortener","category":"Practice","level":"Advanced",
     "summary":"A canonical end-to-end design: encode, store, redirect, scale.",
     "keywords":["url shortener","base62","redirect","key generation","cache"],"refs":["TinyURL design"],
     "overview":"Designing a URL shortener (TinyURL/bit.ly) exercises the whole toolkit: key generation, a read-heavy KV store, caching, and redirects. Reads vastly outnumber writes, so caching and read scaling dominate.",
     "concepts":["Generate short keys via base62 of a counter/ID or hashing.","Store key→URL in a KV store (read-optimized).","Redirect: 301 (cached) vs 302 (trackable).","Cache hot keys; CDN at the edge.","Analytics via async events, not on the hot path."],
     "code":[("text","POST /shorten {url} -> id -> base62(id) = 'aZ3xK' -> store\nGET /aZ3xK -> lookup (cache->DB) -> 302 redirect to url\nRead:Write ~ 100:1  => cache + replicas")],
     "example":{"title":"Key generation choices","text":"Counter+base62 gives short, collision-free keys; random/hash needs collision checks.","code":[("text","counter=125 -> base62 -> '21'   (sequential, predictable)\nhash(url)[:7] -> check collision, retry if taken")]},
     "best":["Use base62 of a unique ID for compact, collision-free keys.","Cache hot mappings aggressively (read-heavy).","Use 301 for cacheable redirects, 302 if you need click tracking.","Offload analytics to a queue.","Add replicas for read scaling."],
     "pitfalls":["Random keys without collision handling.","Putting analytics writes on the redirect hot path.","No caching for a 100:1 read system.","Predictable keys enabling enumeration (if sensitive).","Storing in a write-optimized store for a read workload.","Ignoring custom-alias collisions/abuse."],
     "interview":[("How to generate short keys?","base62-encode a unique counter/ID for compact collision-free keys, or hash+collision-check."),("Why is it read-heavy?","Each created link is read many times — design for read scaling and caching."),("301 vs 302 redirect?","301 is cacheable (less load, no tracking); 302 lets you record each click."),("Where do analytics go?","Off the hot path — emit events to a queue and process asynchronously."),("How to scale reads?","Cache hot keys, add read replicas, and a CDN."),("How long are the keys?","~7 base62 chars give 62^7 ≈ 3.5 trillion combinations."),("Handling custom aliases?","Check uniqueness and reserve/validate them."),("Storage choice?","A read-optimized KV store (Redis/Dynamo) fits the access pattern.")],
     "practice":["Design the API + data model for a shortener.","Estimate storage and QPS for 100M links.","Choose 301 vs 302 and justify."],
     "revision":"URL shortener: base62(id) keys → KV store → cache/CDN → 301/302 redirect; read-heavy so cache+replicas dominate; analytics async via queue. Handle collisions and custom aliases."},

    {"id":12,"slug":"12-design-news-feed","name":"Design: News Feed","category":"Practice","level":"Advanced",
     "summary":"Fan-out on write vs read; the celebrity problem.",
     "keywords":["news feed","fan-out","timeline","push pull","ranking"],"refs":["Feed system design"],
     "overview":"A social **news feed** (Twitter/Instagram) tests the fan-out trade-off: **push** (precompute each follower's timeline on write) gives fast reads but expensive writes; **pull** (assemble at read time) is cheap to write but slow to read. A **hybrid** handles celebrities.",
     "concepts":["Fan-out-on-write (push): precompute timelines; fast reads, heavy writes.","Fan-out-on-read (pull): merge at read time; cheap writes, slow reads.","Celebrity problem: huge follower counts make push explode.","Hybrid: push for most, pull for celebrities.","Rank by recency/relevance; cache hot timelines."],
     "code":[("text","Push:  user posts -> write into each follower's feed cache\nPull:  user opens feed -> fetch + merge recent posts of followees\nHybrid: push normally; for celebrities, pull their posts at read")],
     "example":{"title":"Why hybrid","text":"Pushing a celebrity's post to 100M timelines is infeasible; instead followers pull celebrity posts and merge with their pushed feed.","code":[("text","follower feed = precomputed(push) ∪ pull(celebrity_followees)")]},
     "best":["Use fan-out-on-write for typical users (read-optimized).","Switch to pull/hybrid for high-follower accounts.","Cache materialized timelines.","Paginate with cursors, not offsets.","Rank and trim feeds; store limited timeline length."],
     "pitfalls":["Pure push breaking on celebrities (write amplification).","Pure pull being too slow for active users.","Unbounded timeline storage.","Offset pagination at scale.","Ignoring ranking/freshness.","Recomputing feeds synchronously on the request path."],
     "interview":[("Fan-out on write vs read?","Push precomputes timelines (fast reads, costly writes); pull assembles on read (cheap writes, slow reads)."),("What is the celebrity problem?","Accounts with millions of followers make fan-out-on-write explode; handle via pull/hybrid."),("Why hybrid?","Push for normal users' fast reads, pull for celebrities to avoid massive write fan-out."),("How to paginate a feed?","Cursor/keyset pagination on time/id, not offsets."),("How to keep feeds fresh?","Async fan-out via queues and ranking by recency/relevance."),("Storage for timelines?","A cache/KV store of bounded length per user."),("How to scale reads?","Materialized, cached timelines plus replicas/CDN."),("How is ranking done?","Score posts by recency, affinity, and engagement signals.")],
     "practice":["Compare push vs pull for a 10M-user feed.","Design a hybrid approach for celebrities.","Choose a pagination strategy and justify."],
     "revision":"News feed: push (fast reads/heavy writes) vs pull (cheap writes/slow reads); hybrid for the celebrity problem. Cache timelines, cursor-paginate, rank by recency/relevance, fan-out async."},
  ],
}
