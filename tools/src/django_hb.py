# Django Handbook spec
SPEC = {
  "id": "django", "name": "Django Handbook", "icon": "🎸",
  "tagline": "Master Django for web/backend interviews: models & ORM, views, DRF APIs, auth, migrations, and production performance.",
  "itemNoun": "Topic",
  "levels": ["Beginner", "Intermediate", "Advanced"],
  "categories": [
    {"name": "Core", "level": "Beginner", "icon": "📘"},
    {"name": "ORM & Data", "level": "Beginner", "icon": "🗄️"},
    {"name": "APIs", "level": "Intermediate", "icon": "🔌"},
    {"name": "Auth & Security", "level": "Intermediate", "icon": "🔐"},
    {"name": "Performance & Ops", "level": "Advanced", "icon": "⚡"},
  ],
  "items": [
    {"id":1,"slug":"01-mvt-architecture","name":"Django Architecture (MVT)","category":"Core","level":"Beginner",
     "summary":"Request flow through URLs, views, models, and templates.",
     "keywords":["mvt","request","url","view","template","settings"],"refs":["Django docs: overview"],
     "overview":"Django uses the **Model-View-Template (MVT)** pattern. A request hits the URL dispatcher, which routes to a **view**; the view talks to **models** (the ORM) and renders a **template** or returns JSON. Settings, apps, and middleware wrap the whole cycle.",
     "concepts":["URLconf maps URL patterns to view callables.","Views contain request-handling logic (function- or class-based).","Models define the schema and ORM access.","Templates render HTML; DRF/JsonResponse return APIs.","Middleware processes every request/response (auth, sessions, CSRF)."],
     "code":[("python","# urls.py\nfrom django.urls import path\nfrom . import views\nurlpatterns = [path('books/<int:pk>/', views.book_detail, name='book')]\n\n# views.py\nfrom django.shortcuts import get_object_or_404, render\nfrom .models import Book\ndef book_detail(request, pk):\n    book = get_object_or_404(Book, pk=pk)\n    return render(request, 'book.html', {'book': book})")],
     "example":{"title":"The request lifecycle","text":"Request → middleware → URL resolver → view → model/template → middleware → response. Each app is a reusable package registered in INSTALLED_APPS.","code":[("python","# settings.py\nINSTALLED_APPS = ['django.contrib.admin', 'books', 'rest_framework']")]},
     "best":["Keep views thin; push logic into models/services.","Use named URL patterns and `reverse()` instead of hardcoded paths.","Split settings per environment (base/dev/prod).","Organize code into small, focused apps.","Use get_object_or_404 for clean 404 handling."],
     "pitfalls":["Business logic crammed into views ('fat views').","Hardcoding URLs instead of using names.","Committing secrets in settings.py.","Forgetting to register apps in INSTALLED_APPS.","Misordered middleware breaking auth/CSRF.","Mixing API and template concerns in one view."],
     "interview":[("What is MVT?","Model-View-Template: models = data, views = logic, templates = presentation; URLconf routes requests."),("How does a request flow through Django?","Through middleware, the URL resolver, the view (which uses models/templates), and back out through middleware."),("Function vs class-based views?","FBVs are explicit and simple; CBVs offer reuse via mixins and generic views."),("What is middleware?","Hooks that process every request/response globally (sessions, auth, CSRF, gzip)."),("Why split settings?","To keep secrets out of code and vary config (DB, debug, hosts) per environment."),("What is an 'app' in Django?","A self-contained, reusable module (models/views/urls) registered in INSTALLED_APPS."),("How to avoid hardcoded URLs?","Name patterns and use reverse()/{% url %}."),("Where does CSRF protection live?","In middleware + template token for unsafe methods.")],
     "practice":["Wire a URL → view → template for a detail page.","Use reverse() to build a URL by name.","Split settings into base/dev/prod."],
     "revision":"MVT: URLconf → view → model/template, wrapped by middleware. Keep views thin, name URLs, split settings, organize into apps."},

    {"id":2,"slug":"02-models-orm","name":"Models & the ORM","category":"ORM & Data","level":"Beginner",
     "summary":"Define schema as Python classes; query without raw SQL.",
     "keywords":["model","orm","queryset","fields","relationships"],"refs":["Django ORM docs"],
     "overview":"Django **models** are Python classes that map to database tables. The **ORM** translates Python queries (`Model.objects.filter(...)`) into SQL and returns model instances. Relationships (`ForeignKey`, `ManyToMany`, `OneToOne`) model associations.",
     "concepts":["Each model field maps to a column with a type and constraints.","QuerySets are lazy — evaluated only when iterated/sliced.","Relationships: ForeignKey (M:1), ManyToManyField, OneToOneField.","Managers (objects) are the query entry point; add custom managers.","`null` (DB) vs `blank` (validation) are different."],
     "code":[("python","from django.db import models\nclass Author(models.Model):\n    name = models.CharField(max_length=100)\nclass Book(models.Model):\n    title = models.CharField(max_length=200)\n    author = models.ForeignKey(Author, on_delete=models.CASCADE, related_name='books')\n    published = models.DateField(db_index=True)\n\n# query\nrecent = Book.objects.filter(published__year=2026).order_by('-published')")],
     "example":{"title":"Traversing relationships","text":"Follow related objects with double-underscore lookups:","code":[("python","Book.objects.filter(author__name='Ann')\nauthor.books.all()  # reverse via related_name")]},
     "best":["Set on_delete explicitly for ForeignKeys.","Add db_index to columns you filter/sort on.","Use related_name for readable reverse access.","Keep model methods for row-level behavior; managers for table-level.","Use NUMERIC/DecimalField for money."],
     "pitfalls":["Confusing null (DB) with blank (form validation).","Forgetting on_delete (required) → error.","Evaluating a QuerySet repeatedly (re-hitting the DB).","Storing money as FloatField.","Editing a migration's model without making a new migration.","CharField without max_length."],
     "interview":[("What is a QuerySet and why lazy?","A query representation evaluated only when needed (iteration/len/slice), enabling chaining and fewer DB hits."),("ForeignKey on_delete options?","CASCADE, PROTECT, SET_NULL, SET_DEFAULT, DO_NOTHING — define referential behavior on delete."),("null vs blank?","null controls DB NULL; blank controls form/validation emptiness."),("How to traverse relations in queries?","Double-underscore lookups: filter(author__name=...)."),("How does the ORM map to SQL?","Each model = table, field = column, QuerySet = SELECT; the ORM compiles and executes it."),("What are managers?","Table-level query interfaces (default objects); customize for reusable queries."),("ManyToMany under the hood?","A junction table; Django manages it (or use through= for extra fields)."),("How to add a DB index?","db_index=True on a field, or Meta.indexes for composite indexes.")],
     "practice":["Model Author/Book with a ForeignKey + related_name.","Query books by author name via lookups.","Add a composite index in Meta."],
     "revision":"Models map classes→tables; the ORM returns instances from lazy QuerySets. Set on_delete, index filtered columns, use related_name, mind null vs blank."},

    {"id":3,"slug":"03-queryset-optimization","name":"QuerySet Optimization (N+1)","category":"ORM & Data","level":"Intermediate",
     "summary":"Eliminate N+1 queries with select_related/prefetch_related.",
     "keywords":["n+1","select_related","prefetch_related","only","annotate"],"refs":["Database access optimization"],
     "overview":"The classic Django performance killer is the **N+1 query problem**: looping over objects and lazily fetching a related object each iteration. `select_related` (SQL JOIN, for FK/O2O) and `prefetch_related` (separate query + Python join, for M2M/reverse FK) fix it.",
     "concepts":["select_related: single JOIN for forward FK/OneToOne.","prefetch_related: extra query for M2M/reverse, joined in Python.","only()/defer() limit selected columns.","annotate()/aggregate() push computation to the DB.","Inspect queries with django-debug-toolbar or connection.queries."],
     "code":[("python","# BAD: 1 + N queries\nfor book in Book.objects.all():\n    print(book.author.name)   # a query per book\n\n# GOOD: 1 query (JOIN)\nfor book in Book.objects.select_related('author'):\n    print(book.author.name)")],
     "example":{"title":"Prefetch for many-to-many","text":"One extra query instead of N:","code":[("python","authors = Author.objects.prefetch_related('books')\nfor a in authors:\n    print(a.name, [b.title for b in a.books.all()])  # no extra queries")]},
     "best":["Use select_related for FK/O2O, prefetch_related for M2M/reverse.","Profile with django-debug-toolbar.","annotate counts/sums in the DB, not Python.","Use only()/values() to fetch fewer columns.","Add indexes for filtered/ordered fields."],
     "pitfalls":["N+1 from lazy related access in loops.","Using prefetch where select_related is correct (and vice versa).","count() in a loop instead of annotate.","Loading full objects when values()/only() suffices.","Over-prefetching unused relations.","Assuming the ORM caches across requests."],
     "interview":[("What is the N+1 query problem?","One query for a list plus one per item for a related object — fixed by eager loading."),("select_related vs prefetch_related?","select_related uses a SQL JOIN for FK/O2O; prefetch_related runs a second query and joins in Python for M2M/reverse FK."),("How to count related rows efficiently?","annotate(Count('relation')) so the DB computes it in one query."),("How do you detect query issues?","django-debug-toolbar or len(connection.queries) in tests."),("only() vs defer()?","only() loads just listed fields; defer() loads all but listed — both reduce column I/O."),("Are QuerySets cached?","A QuerySet caches its results once evaluated, but a new QuerySet re-queries."),("aggregate vs annotate?","aggregate returns a single summary dict; annotate adds a per-row computed field."),("When does select_related not help?","For many-to-many/reverse relations — use prefetch_related.")],
     "practice":["Fix an N+1 loop with select_related.","Prefetch a M2M relation and verify query count.","Replace per-row counts with annotate."],
     "revision":"N+1 = list query + per-item related queries. select_related (JOIN, FK/O2O), prefetch_related (2nd query, M2M/reverse), annotate for DB-side counts. Profile with debug-toolbar."},

    {"id":4,"slug":"04-views-cbv","name":"Views: Function & Class-Based","category":"Core","level":"Intermediate",
     "summary":"Handle requests with FBVs or reusable generic CBVs.",
     "keywords":["fbv","cbv","generic views","mixins","get post"],"refs":["Class-based views docs"],
     "overview":"Views turn requests into responses. **Function-based views (FBVs)** are explicit; **class-based views (CBVs)** provide reuse through generic views (ListView, DetailView, CreateView) and mixins. Choose based on how much standard CRUD you can reuse.",
     "concepts":["FBV: a function taking request → response; branch on request.method.","CBV: methods like get()/post(); generic views handle CRUD.","Mixins add cross-cutting behavior (LoginRequiredMixin).","get_queryset/get_context_data customize generic views.","URLconf points to View.as_view()."],
     "code":[("python","from django.views.generic import ListView\nclass BookList(ListView):\n    model = Book\n    paginate_by = 20\n    def get_queryset(self):\n        return Book.objects.select_related('author').order_by('-published')")],
     "example":{"title":"FBV equivalent","text":"The explicit version:","code":[("python","def book_list(request):\n    books = Book.objects.select_related('author')[:20]\n    return render(request, 'books.html', {'books': books})")]},
     "best":["Use generic CBVs for standard CRUD; FBVs for custom flows.","Override get_queryset/get_context_data instead of duplicating logic.","Apply LoginRequiredMixin/permission mixins for access control.","Keep views thin; delegate to services/forms.","Paginate large lists."],
     "pitfalls":["Reinventing CRUD when a generic view exists.","Overusing deep mixin chains that obscure flow.","Forgetting as_view() in URLconf.","Heavy logic in get()/post() instead of services.","Not handling both GET and POST in FBVs.","Mixin ordering (MRO) surprises."],
     "interview":[("FBV vs CBV?","FBVs are explicit and simple; CBVs enable reuse via generic views and mixins for standard patterns."),("How do generic views save code?","They implement common CRUD (List/Detail/Create/Update/Delete) you customize via hooks."),("What are mixins?","Reusable behavior classes (e.g., LoginRequiredMixin) composed into CBVs."),("Where do you customize a ListView's query?","Override get_queryset()."),("How is a CBV wired to a URL?","Via ClassName.as_view() in urlpatterns."),("How to require login on a CBV?","Add LoginRequiredMixin (first) to the class."),("get_context_data use?","Add extra template context in CBVs."),("When avoid CBVs?","Highly custom, non-CRUD logic where explicit FBVs are clearer.")],
     "practice":["Convert an FBV list to a ListView with pagination.","Add LoginRequiredMixin to a CreateView.","Override get_context_data to add stats."],
     "revision":"Views = request→response. FBVs explicit; CBVs reuse via generic views + mixins (override get_queryset/context). Wire CBVs with as_view(); keep views thin."},

    {"id":5,"slug":"05-drf","name":"REST APIs with DRF","category":"APIs","level":"Intermediate",
     "summary":"Build JSON APIs with serializers, viewsets, and routers.",
     "keywords":["drf","serializer","viewset","router","api"],"refs":["Django REST Framework"],
     "overview":"**Django REST Framework (DRF)** is the standard for JSON APIs. **Serializers** validate and convert between models and JSON; **ViewSets** bundle CRUD actions; **routers** auto-generate URLs. It also provides authentication, permissions, throttling, and pagination.",
     "concepts":["ModelSerializer auto-maps model fields to JSON.","ViewSet groups list/create/retrieve/update/destroy.","Routers wire ViewSets to RESTful URLs.","Permissions/authentication classes guard endpoints.","Pagination/filtering/throttling are pluggable."],
     "code":[("python","from rest_framework import serializers, viewsets, routers\nclass BookSerializer(serializers.ModelSerializer):\n    class Meta:\n        model = Book\n        fields = ['id', 'title', 'author']\n\nclass BookViewSet(viewsets.ModelViewSet):\n    queryset = Book.objects.select_related('author')\n    serializer_class = BookSerializer\n\nrouter = routers.DefaultRouter()\nrouter.register('books', BookViewSet)")],
     "example":{"title":"Custom validation","text":"Add field/object validation in the serializer:","code":[("python","class BookSerializer(serializers.ModelSerializer):\n    def validate_title(self, value):\n        if len(value) < 3:\n            raise serializers.ValidationError('Title too short')\n        return value")]},
     "best":["Use ModelSerializer for CRUD; explicit serializers for custom shapes.","Set permission_classes per ViewSet.","Optimize ViewSet querysets (select_related/prefetch).","Paginate list endpoints.","Version your API (URL or header)."],
     "pitfalls":["fields = '__all__' leaking sensitive columns.","N+1 queries inside serializers (optimize the queryset).","Missing permissions leaving endpoints open.","Heavy logic in serializers vs services.","Not validating input, trusting the client.","Returning full objects where a slim serializer suffices."],
     "interview":[("What does a serializer do?","Validates and converts between complex types (model instances) and JSON, both directions."),("ViewSet vs APIView?","ViewSet bundles CRUD actions and pairs with routers; APIView gives explicit method control."),("How are DRF URLs generated?","Routers register ViewSets and produce RESTful URL patterns."),("How to secure endpoints?","authentication_classes (who) + permission_classes (what they can do)."),("How to avoid leaking fields?","List explicit fields, not '__all__'; use read_only/write_only."),("How to optimize API DB access?","Set the ViewSet queryset with select_related/prefetch_related."),("Where to put custom validation?","validate_<field>/validate() in the serializer."),("How to paginate?","Configure DEFAULT_PAGINATION_CLASS or set pagination_class on the view.")],
     "practice":["Expose a model as a ModelViewSet with a router.","Add field-level validation to a serializer.","Add token auth + IsAuthenticated permission."],
     "revision":"DRF: serializers (validate/convert), ViewSets (CRUD), routers (URLs), plus auth/permissions/pagination. List explicit fields, optimize querysets, secure every endpoint."},

    {"id":6,"slug":"06-migrations","name":"Migrations","category":"ORM & Data","level":"Intermediate",
     "summary":"Version-control your schema with auto-generated migrations.",
     "keywords":["migration","makemigrations","migrate","schema","data migration"],"refs":["Migrations docs"],
     "overview":"Migrations are version-controlled, ordered changes to the database schema generated from model edits. `makemigrations` creates them; `migrate` applies them. **Data migrations** transform existing rows alongside schema changes.",
     "concepts":["makemigrations diffs models → migration files; migrate applies them.","Migrations form a dependency graph across apps.","Data migrations use RunPython for transforms.","Reversible migrations support rollback.","Squashing collapses many migrations into one."],
     "code":[("bash","python manage.py makemigrations books\npython manage.py migrate\npython manage.py sqlmigrate books 0002   # preview SQL")],
     "example":{"title":"Data migration","text":"Backfill a column with RunPython:","code":[("python","from django.db import migrations\ndef set_slug(apps, schema_editor):\n    Book = apps.get_model('books', 'Book')\n    for b in Book.objects.all():\n        b.slug = b.title.lower().replace(' ', '-'); b.save()\nclass Migration(migrations.Migration):\n    dependencies = [('books', '0001_initial')]\n    operations = [migrations.RunPython(set_slug, migrations.RunPython.noop)]")]},
     "best":["Commit migrations with the model changes that created them.","Use apps.get_model in data migrations (historical models).","Make migrations reversible where possible.","Test migrations on a copy of prod data.","Add columns as nullable/with default for zero-downtime."],
     "pitfalls":["Editing applied migrations instead of creating new ones.","Importing real models (not historical) in data migrations.","Non-nullable column without a default on a populated table.","Merge conflicts from parallel migrations (use makemigrations --merge).","Long-locking schema changes on big tables.","Forgetting to run migrate in deploys."],
     "interview":[("What problem do migrations solve?","Versioned, repeatable, ordered schema evolution synced with model code."),("makemigrations vs migrate?","makemigrations generates migration files from model diffs; migrate applies pending ones to the DB."),("What is a data migration?","A migration that transforms existing data via RunPython, often alongside schema changes."),("Why use apps.get_model in data migrations?","To get the historical model state at that migration point, not the current code."),("How to add a NOT NULL column safely?","Add it nullable or with a default, backfill, then enforce NOT NULL."),("How to resolve migration conflicts?","makemigrations --merge or reorder dependencies."),("Are migrations reversible?","If operations define reverse logic; some (like data transforms) need an explicit reverse."),("What does squashing do?","Combines a long migration history into fewer files for speed.")],
     "practice":["Add a field and generate/apply a migration.","Write a RunPython data migration to backfill.","Preview a migration's SQL with sqlmigrate."],
     "revision":"Migrations = versioned schema changes (makemigrations→migrate). Use historical models in data migrations, keep them reversible, add columns safely (nullable/default) for zero downtime."},

    {"id":7,"slug":"07-auth","name":"Authentication & Authorization","category":"Auth & Security","level":"Intermediate",
     "summary":"Users, sessions, permissions, and access control.",
     "keywords":["authentication","authorization","permissions","sessions","user model"],"refs":["Django auth docs"],
     "overview":"Django ships a full auth system: a `User` model, session-based login, password hashing, permissions, and groups. **Authentication** verifies identity; **authorization** checks what a user may do (permissions, `@login_required`, mixins).",
     "concepts":["authenticate() + login() establish a session.","Permissions are per-model (add/change/delete/view) + custom.","Groups bundle permissions for roles.","Decorators/mixins enforce access (@login_required, PermissionRequiredMixin).","Use a custom user model from the start if you may extend it."],
     "code":[("python","from django.contrib.auth import authenticate, login\ndef sign_in(request):\n    user = authenticate(request, username=u, password=p)\n    if user is not None:\n        login(request, user)   # sets the session cookie")],
     "example":{"title":"Protect a view","text":"Require login and a permission:","code":[("python","from django.contrib.auth.decorators import login_required, permission_required\n@login_required\n@permission_required('books.add_book', raise_exception=True)\ndef create_book(request): ...")]},
     "best":["Define a custom user model before the first migration.","Never store plaintext passwords (Django hashes them).","Use groups for role-based access.","Enforce permissions server-side, not just in the UI.","Use HTTPS + secure/HttpOnly cookies in production."],
     "pitfalls":["Switching to a custom user model after migrations (painful).","Authorization checks only in templates, not views.","Storing secrets/passwords insecurely.","Confusing authentication with authorization.","Forgetting raise_exception → silent redirects.","Long-lived sessions without expiry/rotation."],
     "interview":[("Authentication vs authorization?","Authentication verifies who you are; authorization decides what you may do."),("How does Django manage login?","authenticate() validates credentials, login() stores the user id in a signed session cookie."),("How are passwords stored?","Hashed with a strong algorithm (PBKDF2/Argon2) and salt — never plaintext."),("What are permissions and groups?","Per-model action permissions; groups bundle permissions to model roles."),("Why a custom user model early?","Changing AUTH_USER_MODEL after migrations is very disruptive."),("How to protect views?","@login_required / LoginRequiredMixin and permission decorators/mixins."),("Session vs token auth?","Sessions for server-rendered apps; tokens/JWT for stateless APIs (often via DRF)."),("Where must authorization be enforced?","On the server in views/serializers — clients can be bypassed.")],
     "practice":["Implement login/logout with sessions.","Restrict a view to a permission.","Create a custom user model with an extra field."],
     "revision":"Auth = identity (authenticate/login, hashed passwords); authz = permissions/groups enforced server-side via decorators/mixins. Use a custom user model from day one; HTTPS + secure cookies in prod."},

    {"id":8,"slug":"08-security","name":"Security: CSRF, XSS, SQLi","category":"Auth & Security","level":"Advanced",
     "summary":"Django's built-in protections and how to keep them on.",
     "keywords":["csrf","xss","sql injection","security","https"],"refs":["Security in Django"],
     "overview":"Django defends against common web attacks by default: **CSRF** tokens on unsafe methods, **XSS** via template auto-escaping, and **SQL injection** via the parameterized ORM. The risks come from bypassing these defaults (raw SQL, mark_safe, disabled middleware).",
     "concepts":["CSRF middleware + {% csrf_token %} protect state-changing requests.","Templates auto-escape variables, preventing reflected XSS.","The ORM parameterizes queries; raw SQL must use params.","Security settings: SECURE_SSL_REDIRECT, HSTS, secure cookies.","Never trust client input; validate with forms/serializers."],
     "code":[("python","# Safe raw SQL — parameters, never string formatting\nBook.objects.raw('SELECT * FROM books_book WHERE title = %s', [title])\n# UNSAFE: f\"... WHERE title = '{title}'\"  -> SQL injection")],
     "example":{"title":"Don't disable escaping blindly","text":"mark_safe / |safe reintroduce XSS if the content isn't sanitized:","code":[("python","# Only mark_safe content you fully control/sanitize\nfrom django.utils.html import escape\nsafe = escape(user_input)")]},
     "best":["Keep CSRF, security middleware, and auto-escaping enabled.","Parameterize any raw SQL.","Set HTTPS, HSTS, secure & HttpOnly cookies in prod.","Validate/sanitize all input via forms/serializers.","Keep Django and dependencies patched."],
     "pitfalls":["Disabling CSRF on POST endpoints.","Using |safe/mark_safe on untrusted content (XSS).","Building SQL with f-strings/% formatting (SQLi).","DEBUG=True in production (leaks internals).","Wildcard ALLOWED_HOSTS.","Storing secrets in code/repo."],
     "interview":[("How does Django prevent CSRF?","A per-session token required on unsafe (POST/PUT/DELETE) requests, checked by middleware."),("How is XSS mitigated?","Templates auto-escape variables; you opt out only with mark_safe/|safe on trusted content."),("How does the ORM stop SQL injection?","It parameterizes queries; values are bound, never concatenated into SQL."),("Risks of DEBUG=True in prod?","Detailed error pages leak settings, stack traces, and SQL — a serious info disclosure."),("What does ALLOWED_HOSTS do?","Restricts which Host headers are served, preventing host-header attacks."),("How to write safe raw SQL?","Use parameter placeholders (%s) with a params list, never string interpolation."),("Key production security settings?","HTTPS redirect, HSTS, SECURE/HttpOnly/SameSite cookies, restricted ALLOWED_HOSTS."),("How to store secrets?","Environment variables / secret managers, not in the repo.")],
     "practice":["Add {% csrf_token %} to a form and test protection.","Rewrite an f-string SQL query to parameterized.","Configure prod security settings (HSTS, secure cookies)."],
     "revision":"Django defaults guard CSRF (tokens), XSS (auto-escape), SQLi (parameterized ORM). Keep them on; parameterize raw SQL; HTTPS/HSTS/secure cookies; DEBUG=False; restrict ALLOWED_HOSTS; secrets in env."},

    {"id":9,"slug":"09-caching-async","name":"Caching & Async Tasks","category":"Performance & Ops","level":"Advanced",
     "summary":"Speed up with caching layers and offload work to Celery.",
     "keywords":["cache","redis","celery","async","queue"],"refs":["Cache framework; Celery"],
     "overview":"Scale Django by **caching** expensive reads (per-view, template fragment, or low-level cache API, often backed by Redis/Memcached) and by moving slow work (emails, image processing) to **background tasks** with Celery so requests stay fast.",
     "concepts":["Cache levels: per-site, per-view, template fragment, low-level API.","Redis/Memcached are common cache backends.","Celery runs tasks via a broker (Redis/RabbitMQ) in workers.","Cache invalidation is the hard part — use keys/TTLs/versioning.","Async views (async def) for IO-bound concurrency (ASGI)."],
     "code":[("python","from django.core.cache import cache\ndef top_books():\n    data = cache.get('top_books')\n    if data is None:\n        data = list(Book.objects.order_by('-sales')[:10].values())\n        cache.set('top_books', data, timeout=300)  # 5 min TTL\n    return data")],
     "example":{"title":"Celery task","text":"Offload a slow email send:","code":[("python","from celery import shared_task\n@shared_task\ndef send_welcome(user_id):\n    # slow IO runs in a worker, not the request\n    ...\n# call: send_welcome.delay(user.id)")]},
     "best":["Cache hot, expensive, rarely-changing reads with TTLs.","Invalidate on writes (signals or explicit cache.delete).","Offload slow/IO tasks to Celery via .delay().","Make tasks idempotent and retry-safe.","Monitor cache hit ratio and queue depth."],
     "pitfalls":["Stale cache from missing invalidation.","Caching per-user data under a shared key.","Doing slow work synchronously in the request.","Non-idempotent tasks duplicating on retry.","Unbounded cache growth without TTLs.","Storing huge objects in cache."],
     "interview":[("What are Django's cache levels?","Per-site, per-view, template fragment, and the low-level cache API."),("Why use Celery?","To run slow/IO-bound work asynchronously in workers so web requests stay fast."),("Hardest part of caching?","Invalidation — keeping cached data consistent with the source of truth."),("How to invalidate on writes?","Delete/update keys on save (signals) or use short TTLs and versioned keys."),("Idempotent tasks — why?","Brokers may deliver/retry tasks more than once; idempotency prevents duplicate effects."),("Sync vs async views?","Async (async def, ASGI) helps IO-bound concurrency; CPU-bound work still needs workers."),("Redis vs Memcached?","Both fast KV caches; Redis adds persistence/data structures and doubles as a Celery broker."),("How to call a Celery task?","task.delay(args) (or apply_async) enqueues it for a worker.")],
     "practice":["Cache a top-N query with a TTL and invalidate on write.","Move an email send to a Celery task.","Add a template fragment cache."],
     "revision":"Cache hot expensive reads (Redis, TTLs, invalidate on write); offload slow work to Celery (.delay, idempotent). Watch hit ratio and queue depth; async views for IO concurrency."},

    {"id":10,"slug":"10-deployment","name":"Deployment & Production","category":"Performance & Ops","level":"Advanced",
     "summary":"Run Django with WSGI/ASGI, static files, and safe settings.",
     "keywords":["gunicorn","wsgi","asgi","static","whitenoise","deploy"],"refs":["Deployment checklist"],
     "overview":"Production Django runs behind a WSGI/ASGI server (Gunicorn/Uvicorn) and a reverse proxy (Nginx). You must set `DEBUG=False`, configure `ALLOWED_HOSTS`, serve **static**/media files properly (WhiteNoise/CDN), and externalize secrets.",
     "concepts":["Gunicorn (WSGI) / Uvicorn (ASGI) run the app; Nginx proxies.","collectstatic gathers static assets; WhiteNoise or a CDN serves them.","DEBUG=False + ALLOWED_HOSTS are mandatory.","Use environment variables for secrets/DB URLs.","Run migrate on deploy; use health checks."],
     "code":[("bash","python manage.py collectstatic --noinput\npython manage.py migrate --noinput\ngunicorn myproject.wsgi:application --workers 3 --bind 0.0.0.0:8000")],
     "example":{"title":"Deployment checklist","text":"Run Django's own check:","code":[("bash","python manage.py check --deploy\n# flags insecure settings: DEBUG, SECRET_KEY, SSL, cookies, HSTS")]},
     "best":["Set DEBUG=False and a strict ALLOWED_HOSTS.","Keep SECRET_KEY and DB creds in env/secret manager.","Serve static via WhiteNoise or a CDN, not Django in prod.","Size Gunicorn workers (~2*cores+1); add a timeout.","Add structured logging, health checks, and monitoring."],
     "pitfalls":["Shipping with DEBUG=True.","Serving static files through Django in production.","Hardcoded secrets in settings.","Too few/many Gunicorn workers.","Forgetting migrate/collectstatic in the deploy pipeline.","No HTTPS/HSTS."],
     "interview":[("WSGI vs ASGI?","WSGI is the sync server interface (Gunicorn); ASGI supports async/websockets (Uvicorn/Daphne)."),("Why DEBUG=False in prod?","DEBUG leaks tracebacks/settings and disables some security; it's an info-disclosure risk."),("How are static files served?","collectstatic gathers them; WhiteNoise or a CDN/reverse proxy serves them efficiently."),("Where do secrets go?","Environment variables or a secret manager — never in code/VCS."),("How many Gunicorn workers?","Roughly 2*CPU+1, tuned by load and request profile; add timeouts."),("What does check --deploy do?","Audits settings for production security issues."),("How to achieve zero-downtime deploys?","Rolling restarts, backward-compatible migrations, health checks, and a load balancer."),("Role of Nginx?","Reverse proxy: TLS termination, static serving, buffering, and load distribution.")],
     "practice":["Run collectstatic + migrate + gunicorn locally.","Run check --deploy and fix flagged issues.","Configure WhiteNoise for static files."],
     "revision":"Prod Django: Gunicorn/Uvicorn behind Nginx, DEBUG=False, strict ALLOWED_HOSTS, secrets in env, static via WhiteNoise/CDN, migrate on deploy, run check --deploy, HTTPS/HSTS, monitoring."},
  ],
}
